# Development History: Artifex

> Index of development entries for this project. Each entry is a separate file in `history/`. This file is the lightweight reference — read individual entries on demand for full details.

## How This Works

- This file is an **index only** — one line per entry with a link to the full entry file
- Full entry content lives in `history/YYYY-MM-DD_NNN_category_slug.md`
- The counter at `history/.counter` tracks the next sequence number
- Update this index whenever a new entry is created

---

## Phase 0

Everything predating this history system. Artifex (working-titled **SWARM** at the time) began as a structured brainstorm (Socratic clarification → 2024–2026 multi-agent research → Six Hats / SCAMPER / premortem) run by Martin Hylleberg, captured in `docs/brainstorms/2026-07-22-agent-swarm.md`. That brainstorm produced a 7-page functional solution dossier in `solution/` (Overview, Architecture, Mission Lifecycle, Agents, Memory & Learning, Observability, Risks & Safeguards), version 1.1 dated 22 July 2026, scoped to functional design only — technology, security, and performance deliberately deferred. No implementation code existed and the repository was not under version control. The claude-project-setup methodology was adopted on 2026-07-22 (entry 001 below).

| # | Date | Category | Summary | File |
|---|------|----------|---------|------|
| 001 | 2026-07-22 | configuration | Adopted claude-project-setup methodology — memory bank, history system, core rules, Tasktracker project | [history/2026-07-22_001_configuration_adopt-methodology.md](history/2026-07-22_001_configuration_adopt-methodology.md) |
| 002 | 2026-07-22 | decision | Chose implementation stack (ADR-0001): all-TS Angular + NestJS + Postgres/pgvector, separate agent-runtime worker, provider-neutral Model Router for multi-model (local + Claude) | [history/2026-07-22_002_decision_implementation-stack.md](history/2026-07-22_002_decision_implementation-stack.md) |
| 003 | 2026-07-22 | configuration | Published as public open-source repo github.com/mhylle/agent-swarm (Apache-2.0) with README + LICENSE | [history/2026-07-22_003_configuration_open-source-repo.md](history/2026-07-22_003_configuration_open-source-repo.md) |
| 004 | 2026-07-22 | decision | Technical/model pass (ADR-0002): 4-tier computed model policy, autonomy-dial budget rule, Postgres Model Catalog (Qwen2.5 v0), Ollama→vLLM serving, TS-now Learning seam | [history/2026-07-22_004_decision_model-tiering-inference.md](history/2026-07-22_004_decision_model-tiering-inference.md) |
| 005 | 2026-07-22 | decision | Named the project **Artifex** (was working-title SWARM); renamed repo → github.com/mhylle/artifex, Tasktracker project, and top-level docs | [history/2026-07-22_005_decision_project-name-artifex.md](history/2026-07-22_005_decision_project-name-artifex.md) |
| 006 | 2026-07-22 | documentation | Finished the rebrand across all content — `solution/` dossier, brainstorm, ADRs (SWARM → Artifex); dated history entries kept as the accurate record | [history/2026-07-22_006_documentation_rebrand-dossier.md](history/2026-07-22_006_documentation_rebrand-dossier.md) |
| 007 | 2026-07-22 | documentation | Registered full architecture in Tasktracker (19 components + 31 relationships) and published contributor-facing `ARCHITECTURE.md` with Mermaid diagrams | [history/2026-07-22_007_documentation_architecture-registered.md](history/2026-07-22_007_documentation_architecture-registered.md) |
| 008 | 2026-07-22 | decision | Formed the v0 plan (ADR-0003/0004): 11 requirements + 26 ACs + 14 TDD phases in Tasktracker; readiness `ready`. TypeBox schemas; 32B-local Tier-2 w/ Claude fallback | [history/2026-07-22_008_decision_v0-plan.md](history/2026-07-22_008_decision_v0-plan.md) |
| 009 | 2026-07-23 | configuration | Adopted the AI Layer: per-package CLAUDE.md hierarchy, SessionStart/Stop hooks, 5 glob-scoped guardrail skills, a working ts-morph codebase-search MCP, and a read-only explorer subagent | [history/2026-07-23_009_configuration_ai-layer.md](history/2026-07-23_009_configuration_ai-layer.md) |
| 010 | 2026-07-24 | code-change | **P0 scaffold (first code):** all-TS npm-workspaces monorepo (5 packages), NestJS 11 api + Angular 20 dashboard, docker-compose (Postgres+pgvector · Redis · Ollama, healthy), testcontainers harness, and CI. Build/test/typecheck green; DoD verified | [history/2026-07-24_010_code-change_p0-scaffold.md](history/2026-07-24_010_code-change_p0-scaffold.md) |
| 011 | 2026-07-24 | dependency | Toolchain bump: Node 24.14 → **24.18.0** (npm → 11.16), unlocking dashboard **Angular 20 → 22** (re-scaffolded; ng update can't cross the hoisted workspace). Angular 22 → vitest+jsdom test builder (drops Karma/Chrome). Full sweep green | [history/2026-07-24_011_dependency_node-angular-upgrade.md](history/2026-07-24_011_dependency_node-angular-upgrade.md) |
| 012 | 2026-07-24 | code-change | **P1 — the five TypeBox schemas** (contract · ledger event · evidence bundle · verdict · capability manifest) + ajv validation core in `@artifex/shared-types`. One object = TS type + validator + LLM JSON Schema, proven by referential identity. TDD red→green; 34 tests; R1 satisfied | [history/2026-07-24_012_code-change_p1-shared-schemas.md](history/2026-07-24_012_code-change_p1-shared-schemas.md) |
| 013 | 2026-07-25 | code-change | **P2 — Memory Fabric data layer** in a new `@artifex/memory-fabric` package (ADR-0005): append-only Audit Ledger (DB triggers reject UPDATE/DELETE/TRUNCATE), monotonic `seq`, `LISTEN/NOTIFY` pointer stream, versioned Model Catalog. TDD red→green; 17 integration tests; dogfooded on the compose stack; R2 satisfied | [history/2026-07-25_013_code-change_p2-memory-fabric.md](history/2026-07-25_013_code-change_p2-memory-fabric.md) |
| 014 | 2026-07-26 | decision | **Four agentic patterns adopted as named principles (ADR-0006)** — audit found planning + multi-agent already core (Gate A verifies the *plan*; Reviewer is a separate agent), reflection partial → **R12**, tool use absent → **R13** (Action Broker). Two constraints are the load-bearing part: self-review is never self-verification, and tool use is brokered, never direct | [history/2026-07-26_014_decision_agentic-patterns.md](history/2026-07-26_014_decision_agentic-patterns.md) |
| 015 | 2026-07-26 | code-change | **P2.5 — R12/R13 contract surface** (ADR-0007): `WorkerContractView` withholds the verification plan *by schema*; `ReflectionRecord` cannot be a verdict by construction; `ActionRecord.viaBrokerGrantId` non-nullable (no unbrokered action); `EvidenceBundle.actions` prose → structured; `action` ledger family. Sequenced before P3 while EvidenceBundle had zero consumers. 66 shared-types tests (was 39); mutation-verified; dogfooded on real Postgres | [history/2026-07-26_015_code-change_p25-contract-surface.md](history/2026-07-26_015_code-change_p25-contract-surface.md) |
| 016 | 2026-07-26 | code-change | **P3 — Model Router & admission gate (ADR-0008)**: shape-only gate admitted EVERY model (constrained decoding guarantees shape, not capability) → added semantic probes; 2b/9b now refused, 12b admitted. Also caught `supportsStructuredOutputs` silently dropping the schema, and enabled the compose GPU block (CPU couldn't finish 2 probes in 10 min). 13 tests, 84 repo-wide | [history/2026-07-26_016_code-change_p3-model-router.md](history/2026-07-26_016_code-change_p3-model-router.md) |
| 017 | 2026-07-27 | code-change | **P3.5 — tier-scoped admission**: P3 probed every candidate with meta-agent artifacts (Verdict/CapabilityManifest), refusing Tier-1 models for a job they'd never be given. Probes now carry `logicalTier`; tier 1 judged on `EvidenceBundle` instruction-following; a tier with no probes raises instead of admitting. qwen3.5:2b passes tier 1 — 2/3 runs. Exposed high defect `d678cd8c`: admission verdicts don't replicate | [history/2026-07-27_017_code-change_p35-tier-scoped-admission.md](history/2026-07-27_017_code-change_p35-tier-scoped-admission.md) |
| 018 | 2026-07-30 | code-change | **P4 — Tier Policy engine (floor-first) & Constitution guards**: tier computed cheapest-first; floor from blast/irreversibility/fan-in is constitutional and budget pressure escalates to a human rather than under-provisioning. Review independence = absolute for AGENT, required for MODEL only above low blast (so bulk review stays cheap). Dogfooded the full chain on real models: tier 1 → qwen3.5:2b, proven clade downgrades 2→1, high-blast root → Claude. 113 tests repo-wide | [history/2026-07-30_018_code-change_p4-tier-policy-constitution.md](history/2026-07-30_018_code-change_p4-tier-policy-constitution.md) |
| 019 | 2026-07-30 | code-change | **P5 — Orchestrator (decompose · contract · fold-up)**: `decompose()` refuses ungradeable / overlapping / unaffordable proposals and DIVIDES the budget; `foldUp()` reconciles rather than concatenates and surfaces sibling contradictions. Model-backed planner + reconciler. Dogfooded on qwen3.5:2b — 3 clean subtasks, all valid contracts, planted conflict detected. 125 tests repo-wide | [history/2026-07-30_019_code-change_p5-orchestrator.md](history/2026-07-30_019_code-change_p5-orchestrator.md) |
| 020 | 2026-07-30 | code-change | **P6 — Agent Creator (reuse-first) + Asset Registry** (migration 0002): staffing requires an EVIDENCE BAR — one lucky run is not a track record; tier computed not chosen; validation harness built from the contract’s own criteria; losers down-weighted, never deleted. Dogfooded the full ratchet: no-bid → gemma4:12b, earned evidence → reuse → qwen3.5:2b. 136 tests, 29 integration | [history/2026-07-30_020_code-change_p6-agent-creator.md](history/2026-07-30_020_code-change_p6-agent-creator.md) |
| 021 | 2026-07-30 | code-change | **P7 — Reviewer (Gate A · Gate B · structured verdicts)**: both gates iterate the CONTRACT’s criteria, never the judge’s answer — an unassessed criterion is a failure, never an implicit pass, and a judge inventing criteria is refused. Uncovered → `specification_fault`, so the ladder re-decomposes. Immutability proven against the real ledger (raw-SQL edit + delete both rejected). Dogfooded on gemma4:12b. 149 tests | [history/2026-07-30_021_code-change_p7-reviewer.md](history/2026-07-30_021_code-change_p7-reviewer.md) |
| 022 | 2026-07-30 | code-change | **P8 — Context Broker & Worker Swarm**: the grant IS the capability, bound to one source, so bypassing the broker is a missing argument rather than a rule to remember. Denials logged as well as grants. Specialist validates it got a `WorkerContractView`; bouncing does no work. **Finding: bigger models over-bounce** — 2b discriminated clear-vs-vague, 9b/12b bounced everything. 162 tests | [history/2026-07-30_022_code-change_p8-context-broker-swarm.md](history/2026-07-30_022_code-change_p8-context-broker-swarm.md) |
| 023 | 2026-07-30 | code-change | **P8.5 — Action Broker (R13 runtime)**: one door for every invocation — successes, failures and refusals alike. Three denial paths, each typed AND logged (the write-class test tool throws if reached, so a silent permission surfaces as the wrong error). `blastRadius` bounds reachable tools; autonomy dial gates ratification. **R13 now complete** across P2.5+P8.5. Dogfooded with a real Postgres-querying tool. 177 tests | [history/2026-07-30_023_code-change_p85-action-broker.md](history/2026-07-30_023_code-change_p85-action-broker.md) |
| 024 | 2026-07-30 | code-change | **P8.6 — self-critique pass (R12 runtime)**: `gateBRequired` is an unconditional literal `true`; record has no gate/outcome/verdictId; filed under `execution`, never `verification`. **R12 complete; both ADR-0006 gaps closed.** ⚠ Dogfood exposed HIGH defect `cd677737` — the model diagnosed correctly but *repaired destructively* (22% → 5%, broke a criterion it had just marked met). Reflection that regresses is worse than none; unconditional Gate B is what catches it. 189 tests | [history/2026-07-30_024_code-change_p86-self-critique.md](history/2026-07-30_024_code-change_p86-self-critique.md) |
| 025 | 2026-07-30 | code-change | **P9 — the mission loop; Artifex runs END TO END.** decompose → Gate A → staff → execute → Gate B → fold-up, delivered live in 78s on real models with 11 ordered events on real Postgres. Hardened so a throwing seam climbs the ladder instead of killing the mission. Live run found 2 bugs — both in my harness, not the loop (worker wasn't shown its criteria; planner ran below ADR-0002's tier for root decomposition). ⚠ HIGH defect `8b7e9e95`: models run away on complex nested schemas. 208 tests | [history/2026-07-30_025_code-change_p9-mission-loop.md](history/2026-07-30_025_code-change_p9-mission-loop.md) |
| 026 | 2026-07-30 | code-change | **P10 — Control Plane API**: intake refuses rather than defaults; task zero validated before anything is recorded; human intake is a first-class ledger event; live stream **hydrates by `seq`** (NOTIFY is a pointer) and a late subscriber replays first. No "run" endpoint — the API enqueues and stops. Moved `api` from jest → **vitest** (+ `unplugin-swc` for Nest DI metadata). Dogfooded on real Postgres + Redis + NOTIFY. 224 tests | [history/2026-07-30_026_code-change_p10-control-plane-api.md](history/2026-07-30_026_code-change_p10-control-plane-api.md) |
| 027 | 2026-07-30 | code-change | **P11 — Learning seam**: projection narrows any input to `replay` only, and read-only is proven BEHAVIOURALLY (handed a repo that HAS append, never calls it). `CONSTITUTIONAL_CORE` is frozen, not commented. A proposal may TARGET the constitution — arguing is the amendment protocol — but there is no apply/amend/adopt method. Dogfooded on the real ledger: count unchanged, constitution byte-identical. 234 tests | [history/2026-07-30_027_code-change_p11-learning-seam.md](history/2026-07-30_027_code-change_p11-learning-seam.md) |
| 028 | 2026-07-30 | code-change | **P12 — Mission Control; THE FRONTEND GAP IS CLOSED.** The task tree is a PURE function of the event list — no store, no view state — so "a view, never a second truth" has nowhere to hide. Status derived from the last verdict, never stored; sorted by `seq` (a websocket isn't ordered). First mutation changed nothing → distractor was vacuous; re-ran modelling an *accumulating* cockpit and it bit. 16 dashboard tests, 246 repo-wide, serves HTTP 200 | [history/2026-07-30_028_code-change_p12-dashboard.md](history/2026-07-30_028_code-change_p12-dashboard.md) |
| 029 | 2026-07-30 | code-change | **P13 — end-to-end dogfood: ARTIFEX WORKS. 20/20.** intake → Redis queue → mission loop on real models → real Postgres ledger → cockpit tree, DELIVERED. Smallest-model-first plus BOTH tier directions proven in one run. Shipped `createStepwisePlanner` first (flat schema, one subtask per call) to mitigate the runaway defect. ⚠ Run 1 of identical code surrendered 18/20 on a stochastic leaf failure — every mechanism degraded correctly; logged as defect `626f6596` (failure probability grows with fan-out) | [history/2026-07-30_029_code-change_p13-end-to-end-dogfood.md](history/2026-07-30_029_code-change_p13-end-to-end-dogfood.md) |
| 030 | 2026-07-30 | code-change | **ALL FIVE DONENESS PILLARS PASS — Artifex is done.** Closed the lifecycle phases, registered the missing components, accepted all 29 ACs against evidence, and FIXED all 5 defects: reflection self-consistency guard; transient retry before a rung is spent; admission samples N times and carries a pass RATE; ledger reads below the xmin commit horizon (reproduced with two real transactions first). 258 unit + 31 integration | [history/2026-07-30_030_code-change_defects-closed-project-done.md](history/2026-07-30_030_code-change_defects-closed-project-done.md) |
| 032 | 2026-07-30 | bug-fix | **The live ledger stream was never connected — and the dossier's scope was never in Tasktracker.** Shipped R14 (start a mission from the cockpit) and fixed 3 defects only a browser could find: CORS unset (jsdom can't see it), intake bodies unvalidated (`fd345eae`, erased TS interface → 500), and `LedgerStreamService.onNotification` **never called by anything** (`b3b4e554`) — a mission ran to completion with a connected dashboard stuck at 1 event. Added `LedgerLiveBridge` + an `onEvent` hook so the worker appends as it happens. Transferred the frozen dossier into **R14–R40, 85 ACs**; 2 of 3 memory-fabric stores and both learning loops had never been specced | [history/2026-07-30_032_bug-fix_live-ledger-stream-connected-and-cockpit-intake.md](history/2026-07-30_032_bug-fix_live-ledger-stream-connected-and-cockpit-intake.md) |
| 033 | 2026-07-30 | code-change | **The operator console becomes real — R14 intake · R21 fleet · R15 canvas · R16 inspector — and 7 defects the BROWSER found.** Live ledger stream was never connected (`b3b4e554`: onNotification well-tested, nothing ever called it); CORS unset (jsdom can't see it); intake 500 on malformed bodies; decomposition never recursed (`a910ed8d`, ADR-0009); planner hardcoded one criterion per child so recursion could never fire (`5e245281`); ledger never recorded criteria/effort/agent-version so "3 of 4 met" had no denominator (`f46ba357`). Clarity-judge fix chosen by MEASUREMENT (2b 33% false-bounce · 9b 17% · 12b 58%) — "raise the tier" was wrong twice over | [history/2026-07-30_033_code-change_operator-console-r14-r21-r15-r16.md](history/2026-07-30_033_code-change_operator-console-r14-r21-r15-r16.md) |
| 034 | 2026-07-30 | code-change | **The cockpit acts, budgets bind, the human enters the loop, missions resume.** R17 (pause/cancel/grant/dial/annotate as ledger events) · R18 (attention queue) · R41 (resume by replaying the trail). Six defects, each exposed by the previous fix: budget ceiling recorded but NEVER enforced (`9fbee9d6`); `humanAt` inherited by every child and read by nobody, so no human was ever asked (`607a2468`); the dial written and never read (`0d39d84b`); missions unresumable (`3be8831e`); the human rung honoured on 1 of 3 escalation paths (`20878859`); and **critical** — event ids from a per-run counter, so a RESUMED run collided with the original and recorded NOTHING while reporting success (`5236850d`) | [history/2026-07-30_034_code-change_cockpit-acts-and-missions-resume.md](history/2026-07-30_034_code-change_cockpit-acts-and-missions-resume.md) |
| 035 | 2026-07-30 | bug-fix | **The mission loop took an instant where it needed a clock** (`74950cfc`). `runMission` captured `now` once per run and stamped every event with it, so a 45-second mission recorded twenty events that all claimed the same moment — the R19 timeline drew correct lanes with every duration `0s`. Now `now: () => string`, read per event. Mutation-checked: restoring the bug kills exactly the two intended tests. Browser-verified — 17 distinct timestamps over 20 events, lanes reading waited 3s/11s/19s against ran 7s/8s/7s. Its first honest reading immediately exposed the sequential-execution stall, which is now R32's measured justification | [history/2026-07-30_035_bug-fix_clock-not-instant.md](history/2026-07-30_035_bug-fix_clock-not-instant.md) |
| 036 | 2026-07-30 | code-change | **Time travel (R20) — the past is a truncated event list, not a snapshot.** A scrubber parks the cockpit on any recorded moment and canvas, inspector, budgets, workforce and all four lenses re-render as they stood; two moments can be compared; the past is read-only, guarded in the method and not only the template. Small because the architecture had already paid for it: the feed stores only raw events and derives the rest, so no snapshot store was needed — and none may ever be added, since a snapshot can disagree with the ledger. 29 tests, 8 mutants all killed, browser-verified at #662/#663/#672 against a real mission. Also corrected five requirements (R14, R16, R18, R21, R41) left `draft` despite every AC being satisfied | [history/2026-07-30_036_code-change_time-travel.md](history/2026-07-30_036_code-change_time-travel.md) |
| 037 | 2026-07-30 | code-change | **Audience scoping (R22), and a doneness gate that had been measuring the wrong repository.** `scanArchitectureDrift`/`getProjectDoneness` default `repoRoot` to the MCP server's own cwd — the gate had been inventorying *Tasktracker's* backend and reporting 241 drift entries for files Artifex does not have. Real drift was 10; now missing 0, stale 0. Six orphaned entries are **carried, not worked around**: a probe proved the walker inventories only NestJS-convention filenames (7 of 142 files), so accurate Angular/worker references are permanently "drift", and zeroing it would mean deleting truthful architecture. R22 itself: a pure audience→scope function whose `mayAct` fails closed, plus a requester projection that reports progress against the MISSION contract's criteria — possible only because criteria are partitioned, not invented. AC-1 left **unsatisfied**: no event carries flagged assumptions (defect `d0d555db`). 27 tests, 6 mutants killed, all three audiences browser-verified | [history/2026-07-30_037_code-change_audience-scoping.md](history/2026-07-30_037_code-change_audience-scoping.md) |
| 038 | 2026-07-30 | code-change | **Parallel execution across the dependency graph (R32 AC-0) — the stall the timeline measured is gone.** `runSubtree`'s per-child body extracted into `runChild`; children now run in dependency-satisfied waves. An edge is satisfied by passing **Gate B**, not by finishing; an empty wave surrenders naming the blocked tasks rather than hanging; verified siblings survive a failing one; fold-up keeps declaration order. Gate A refuses cycles via on-stack DFS — a plain seen-set would reject a diamond. Browser-verified: waits went **3s/11s/19s → 5s/5s/5s**, all three tasks staffed within 24ms. 8 tests, 496 green, 5 mutants killed — and the fold-order mutant initially SURVIVED, exposing an untested determinism claim. AC-1/AC-2 left **unsatisfied**: the planner still never declares an edge (defect `11befcf8`) | [history/2026-07-30_038_code-change_parallel-execution.md](history/2026-07-30_038_code-change_parallel-execution.md) |
| 039 | 2026-07-30 | code-change | **The planner declares edges — R32 and R15 both close.** `SubtaskDependencySchema` (flat `{dependsOn: number[]}`, `-1` = independent) asks the model which siblings feed which; indexes are remapped so a dropped subtask cannot misaim an edge, self-edges and out-of-range are dropped, and a real cycle is carried through so **Gate A** can refuse it rather than the planner silently mangling the plan. Found and fixed a crash on a malformed model reply. Canvas now NAMES each edge instead of counting them (R15 AC-0). Browser-verified on a "write then critique" mission: qwen3.5:2b declared the edge unprompted and the consumer was staffed **2 ms** after the producer's Gate B pass; timeline shows waited 3s vs 10s. 8 tests, 508 green, 12 mutants killed incl. the unwired-input shape | [history/2026-07-30_039_code-change_declared-dependency-edges.md](history/2026-07-30_039_code-change_declared-dependency-edges.md) |
| 040 | 2026-07-30 | testing | **The graceful pause, finally verified — by changing the instrument, not retrying the click.** R17 AC-1 had resisted three browser attempts (friction `63aff355`): a tier-1 task outruns the click, so the pause always landed *before* the attempt, demonstrating the opposite of the criterion. The R32 **latch** — a `work` seam that blocks until released — holds an attempt open so the pause arrives *during* it. Asserts the in-flight attempt still produced `task.executed` and a verdict, `task.paused` came at the NEXT boundary, and `work` ran exactly once. No production code changed: this was a verification gap. All four tests passed on first run, so mutation checking was the only real check — the decisive mutant discards a completed attempt and killed 2. R17 satisfied, P17 closed, 512 green | [history/2026-07-30_040_testing_graceful-pause-verified.md](history/2026-07-30_040_testing_graceful-pause-verified.md) |
| 041 | 2026-07-30 | code-change | **The decompose-or-delegate gate (R31 AC-0/AC-1) — atomization becomes a decision, not a reflex.** Every node now records a split/keep-whole verdict with its rationale, *including* the default when no gate is configured. Kept-whole work goes to one agent on the node's FULL budget (derived: an unsplit node keeps its own ceiling instead of dividing it). Gate A is skipped — it grades a decomposition — but Gate B still runs. The trap guarded: `asLeaf` stops a kept-whole node being re-split one level down. Live: a limerick was kept whole (0 children, ceiling 30), two unrelated tools split (2 children, 12 each), each with the model's own reasoning. Surfaced and fixed a canvas that said "No tasks contracted yet" on a mission that had run three attempts. 12 tests, 521 green, 12 mutants killed. AC-2 **unsatisfied** — needs the Asset Registry (R23), defect `68f6c31c` | [history/2026-07-30_041_code-change_decompose-or-delegate-gate.md](history/2026-07-30_041_code-change_decompose-or-delegate-gate.md) |
| 042 | 2026-07-30 | code-change | **The Asset Registry's ratchet (R23) — assets advance one validated delta at a time.** Orienting found a partial registry where **none** of the three criteria held: `upsert` was a wholesale rewrite with no evidence link, there was no ratchet, and retirement *excluded* rather than down-weighted. Migration 0004 adds `agent_design_delta`; "only measured wins enter" is a DB constraint, not a convention; `proposeDelta` runs `FOR UPDATE` so two proposals can't both adopt. Reverted deltas are recorded (else the same losing change returns forever); an unproven incumbent is not a tie; simplicity is derived from the asset; "reduced weight" is ordering, not an invented multiplier. 52 integration tests green, 6 mutants killed one-for-one. **Two honest exceptions:** no browser evidence exists for this requirement, and the registry is currently **inert** — `runtime.ts` stubs `bestForCategory` to null and nothing records outcomes (defect `41f7555c`, closes with R28/R38). Also corrects the plan: R23 does *not* unblock R31 AC-2 — that needs decomposition templates, not agent designs | [history/2026-07-30_042_code-change_asset-registry-ratchet.md](history/2026-07-30_042_code-change_asset-registry-ratchet.md) |
| 043 | 2026-07-30 | code-change | **The reuse market goes live (R38 AC-1)** — "reuse first, creation second" was broken at THREE points, any two of which fixed alone still leaves a system that always authors afresh: `designIdFor` derived from `taskId` not category (its own comment disagreed with it), authored designs were never persisted, and no outcome was ever recorded so the evidence bar was unreachable. Design ids are now a category hash (open taxonomy → hash, not lookup table); staffing registers what it authors; Gate B's verdict folds into the track record (pass=1/fail=0, derived); `index.ts` passes the real repository. Live: five identical missions → runs 1–3 author onto ONE row, runs 4–5 **reuse** it at v3, browser-confirmed in the workforce lens. Resolves `41f7555c`. An existing test had encoded the defect and was rewritten in place. Surfaced two new items: `upsert` bumps the version with no delta (`fe690036`), and the composition root is untested — the unwired-input mutant only failed to compile by accident | [history/2026-07-30_043_code-change_reuse-market-live.md](history/2026-07-30_043_code-change_reuse-market-live.md) |
| 044 | 2026-07-30 | refactor | **Registration stops pretending to be a ratchet, and the composition root becomes testable.** `upsert` bumped `version` on every re-registration (`fe690036`); now `DO NOTHING` + read-back — a conflict changes nothing at all, because a no-bid overwriting an incumbent is a rewrite without evidence. `staff()` reports the stored version, so ledger and registry agree. Live: design held at v3 while observations climbed to 7. Second fix: assembly extracted to `buildWorkerSeams`, so the mutant that started this — passing no registry — now fails **4 tests** instead of only tripping an incidental `TS6133`; a further test asserts `main()` actually uses it, guarding against recreating `04071ce9`. 11 tests, 541 green. **Pattern named:** for the second iteration running an existing test encoded a defect as a requirement — the tell both times was a test asserting a *mechanism* where its describe named a *property* | [history/2026-07-30_044_refactor_composition-root-asserted.md](history/2026-07-30_044_refactor_composition-root-asserted.md) |
| 045 | 2026-07-30 | code-change | **Earned permanence (R28 AC-1/AC-2) — the clade the comment always described.** `clade_score` claimed "how this LINEAGE performed" since P6 while the table had no ancestry: it was a per-design mean. Migration 0005 adds `parent_design_id`, `mean_effort`, `validation_harness`; clade is now an observation-weighted recursive CTE (cycle-safe), the Pareto front keeps cheap-but-adequate alongside costly-but-better, and promotion is refused for a design with no harness — on *measurability*, not performance. 13 tests, 7 mutants killed, 67 integration + 542 unit green. **Footgun that cost two live missions:** `npm run build -w packages/worker` leaves a changed `memory-fabric` stale, so correct wiring wrote NULLs and looked like a SQL bug — tell: green tests + correct dist + wrong data ⇒ stale sibling package. AC-0 left **unsatisfied** (`cb939996`): nothing ever sets `parent_design_id`, so no live design has ancestors | [history/2026-07-30_045_code-change_earned-permanence.md](history/2026-07-30_045_code-change_earned-permanence.md) |
| 046 | 2026-07-30 | code-change | **The early surrender signal (R38 AC-3), and clustering that does not yet cluster.** A capability audit runs after Gate A and *before any task executes*: when every child no-bids, `staffing.capability_gap` names the unserved capabilities. A warning, never a refusal — a first mission in a new domain no-bids on everything by definition. Live: 3 no-bids of 3, raised before the first `task.executed`. **AC-0 left unsatisfied, and the live run is why:** `capabilityOf` normalises category strings (10 categories → 3 designs in a fixture, mutants killed) but the planner named all three subtasks differently, so a real mission still got 3 designs for 3 tasks. Passive normalisation cannot cluster what was never named alike; active matching against known capabilities is the missing piece (`eee34306`). A fixture proved the function; only real data proved the feature was absent. 12 tests, 508 unit + 67 integration green | [history/2026-07-30_046_code-change_capability-gap-signal.md](history/2026-07-30_046_code-change_capability-gap-signal.md) |
| 047 | 2026-07-30 | bug-fix | **The gate must be repeatedly confident before it collapses a task graph** (`890cdea5`). Found by accident while measuring R38 AC-0: a five-criterion mission contracted ZERO tasks because the gate answered `keep_whole` while its own rationale said *"five independent descriptions… no shared reasoning"* — one agent took all five, bounced, escalated, **surrendered**. No schema catches a confident wrong answer; repetition does. `sampledDecompositionGate` now requires **unanimity across three samples**, following the admission gate's existing precedent (`d678cd8c`); a throwing sample counts as dissent, and the dissenting rationale is what gets recorded. Live before/after on identical text: keep_whole/0 tasks/surrendered → **split/5 tasks**. 6 tests, 4 mutants killed, 270 worker green. AC-0 still unsatisfied but with sharper evidence: five categories that are plainly one capability still yielded five designs | [history/2026-07-30_047_bug-fix_gate-unanimity.md](history/2026-07-30_047_bug-fix_gate-unanimity.md) |
| 048 | 2026-07-30 | code-change | **Active clustering (R38 AC-0) — five tasks, one design.** Normalising a category cannot merge names the planner never repeats, and it never repeats them. `resolveCapability` now matches each proposal against capabilities the registry already holds, supplied **evidence-ordered** so a proposal joins the better-established one; staffing bids and registers under the resolved capability, making the registry's distinct categories the taxonomy. Errs toward merging deliberately — the evidence bar catches a wrong reuse, but a taxonomy growing per-task can never accumulate evidence. Identical mission text: **5 designs → 1**, with that design reaching 5 observations from one mission. A **surviving mutant** found a real gap (staffing bidding on the raw category would silently revert reuse while all tests stayed green) — third time mutation has caught an untested claim. Two tests rewritten in place, mechanism-vs-property, fourth occurrence. 8 tests, 278 worker + 67 integration green. AC-2 remains, deliberately unstarted rather than half-done | [history/2026-07-30_048_code-change_active-clustering.md](history/2026-07-30_048_code-change_active-clustering.md) |
| 049 | 2026-07-30 | code-change | **The design playbook and effort scaling — R38 COMPLETE.** The author seam was one hardcoded template string; `composeDesign` now emits five named blocks (role/scope/evidence/anti_scope/stopping) each filled from a contract field, so the space is constrained *by construction* rather than by asking a model nicely. Absent anti-scope prints "none stated" — a blank reads as "nothing is out of scope". Capabilities derived, not fixed. `concurrencyFor` bounds each wave by budget (worker floors vs parent ceiling) and blast radius (a fraction of the wave), never returning 0. **Live:** a composed design visible in the registry, and three high-risk tasks staffed one at a time ~7s apart against R32's 24ms baseline. A test that couldn't be written as intended is recorded: the budget bound cannot bite through the loop because child floors are capped at `effortShare` of the parent ceiling. 13 tests, 6 mutants killed incl. both composition mutants, 292 worker + 67 integration green. Also derived `fanIn`/`budgetHeadroom`, StaffOptions fields previously supplied by nobody | [history/2026-07-30_049_code-change_design-playbook.md](history/2026-07-30_049_code-change_design-playbook.md) |
| 050 | 2026-07-30 | code-change | **The Knowledge Commons (R24) — guilty until proven useful.** The third store, genuinely unbuilt. Its three rules are DATABASE constraints, not conventions: sourced (no anonymous knowledge — provenance and non-empty evidence required to insert), earned (closed status set, nothing born published, and no admission path that skips quarantine), mortal (`published` implies an expiry). Judgement calls recorded: quarantined findings are SERVED with an `unproven` label rather than withheld; an expired entry stays retrievable but `current: false`, since hiding it reads as "nobody ever found this"; low-impact publishes on its own provenance because requiring a stranger for everything means nothing is ever shared. 14 tests, 81 integration green. **A surviving mutant** exposed the `publish` corroboration filter as untested defence-in-depth — a self-corroboration planted by direct write would have passed; now covered. **Store is INERT** (`753bc6dd`): no producer, no consumer, and wiring retrieval alone was deliberately refused since the broker would consult an empty store. R40 is the producer. No UI surface, same standing exception as R23 | [history/2026-07-30_050_code-change_knowledge-commons.md](history/2026-07-30_050_code-change_knowledge-commons.md) |
| 051 | 2026-07-30 | code-change | **R40 — the worker contract ritual, and what only the live stack could tell us.** `task.executed` now carries `actions`/`consulted`/`assumptions` beside the deliverable (empty recorded AS empty — absent and empty are different claims), and the effort floor binds before Gate B, because the floor is a claim about effort and the reviewer does not measure effort; below-floor work costs a rung, not the task. Ten tests and six dead mutants proved the plumbing — then the live stack found **two things no test could**. (1) **The producer did not exist**: `createMissionSeams` hardcoded `assumptions: []`, so the field was perfect and permanently empty, the same inert shape as the Commons one iteration earlier; a new test drives the real seam and asserts it ASKS. (2) **Defect `08db92fd`**: `qwen3.5:2b` emits JSON syntax INSIDE the answer string (`"5\", \"explanation\": ...`), passing schema validation because it is a non-empty string — a silent-wrong-answer path Gate B then judges. **A misattribution recorded rather than buried:** the corruption was first blamed on schema width and reproduced afterwards on the single-field schema — the comment and the test encoding that false cause were corrected in place, since a test asserting a wrong cause would have closed the real investigation. A **fifth surviving mutant** caught a test whose describe promised the prompt taught a concept while the assertion only checked the word appeared. Browser-verified: a real worker's declared assumptions render in the requester lens. **Honest caveat:** `effortSpent` is still a hardcoded 1, so effort is not yet MEASURED. Resolves `d0d555db`; R40 and R22 both satisfied | [history/2026-07-30_051_code-change_worker-contract-ritual.md](history/2026-07-30_051_code-change_worker-contract-ritual.md) |
| 052 | 2026-07-30 | bug-fix | **A deliverable that is serialised JSON, not an answer (`08db92fd`).** The value IS a non-empty string, so the schema passes, Gate B reads garbage as prose and may find the criterion met, and the requester view renders it — every layer behaving correctly on input that is nonsense, which is worse than a crash. **Rate measured, not estimated:** replaying every `task.executed` in the ledger gives **2 of 65 (~3%)**, in two shapes taken verbatim into the tests — a truncated JSON document where prose was asked for, and a fragment where the model closed the string and kept authoring keys. Fixed in the same shape as R40's effort floor because it is the same class of failure: record `task.malformed_deliverable` and climb a rung, BEFORE Gate B; a corrupt attempt costs a rung, not the task, since at 3% a retry overwhelmingly returns something good. Deliberately NOT fixed in the model-router — "a string must not contain JSON" is false in general. **The falsification that mattered:** fixtures prove a detector does what you wrote, not that it leaves good work alone, so it was replayed over all 65 real answers — flagging exactly the 2 known-bad and none of the 63 good. **Sixth surviving mutant:** dropping the quoted-key half of the pattern passed every test, yet fires on prose listing quoted items (`"toast", "soldiers"`). **Limitation stated, not hidden:** a task legitimately asking for JSON trips the document detector — only intent separates them, and this seam does not parse intent; accepted because one rung is cheap against a silent wrong answer. **Could NOT be verified live** — 8 of 8 raw calls with the failing prompt came back clean, so the detector is proven by historical replay, not live reproduction. Also closed stale phase P22 | [history/2026-07-30_052_bug-fix_malformed-deliverable.md](history/2026-07-30_052_bug-fix_malformed-deliverable.md) |
| 053 | 2026-07-30 | code-change | **The Knowledge Commons gets a producer — and the consumer turns out to be blocked on something bigger.** A task passing Gate B now submits its finding; buildable only because R40's evidence bundle gave a verified task provenance and evidence ids to cite. Four judgements recorded IN the tests: the claim is the verified deliverable keyed to its objective (a model-extraction seam would be a new thing to be wrong about, and quarantine exists precisely so the producer need not be a perfect judge); Gate B's pass is the admission ticket (submitting at execution fills the store with what quarantine exists to keep out); impact is DERIVED from the producing task's blast radius, with anything not `high` mapping to `low` because the DB's impact is a closed two-value set; a commons failure is swallowed, since losing verified work over a bookkeeping write trades the product for the receipt. **The finding of the iteration is what was NOT built:** `new ContextBroker` appears nowhere outside its own test file, so invariant #6's sole context channel is never instantiated (defect `488709be`) — wiring a consumer into it would be the same inert shape one level up. **Near-miss:** making `record()` return its event id landed the `return` before `onEvent`, silently regressing live streaming (`b3b4e554`); the existing streaming tests caught it. 6 mutants killed, 329 worker + 66 green. **Proven on the real stack:** `knowledge_entry` 0 rows → 1, quarantined/gate_b, and both evidence ids resolve against `ledger_event` to the actual `task.executed` and `gate_b.verdict_issued` rows. A test was corrected at its input rather than bent to match | [history/2026-07-30_053_code-change_knowledge-commons-producer.md](history/2026-07-30_053_code-change_knowledge-commons-producer.md) |
| 054 | 2026-07-30 | code-change | **Gate A in full — and two live rejections that were the gate's fault, not the plan's.** Gate A audited 2 of 6 dossier clauses; now 5, and a rejection feeds a bounded aimed re-split (`rejectedBecause`) instead of surrendering the subtree. Overlap started DETERMINISTIC and the existing suite correctly rejected that — a criterion is routinely met jointly, so shared coverage says nothing about overlap; it moved to the judge. The judge is REQUIRED, which immediately made the worker refuse to compile until the runtime supplied a real one. **A clause nothing could satisfy:** pinned decisions are INHERITED with no way for the planner to propose one, so every dependent plan would have been rejected forever — decomposition now derives the pin from the edge the plan already declares. **Then the live stack rejected everything twice, both the gate's fault:** testability was judged on criteria the planner cannot reword (criteria are partitioned, never invented — it rejected the requester's own intake words and surrendered), and the tier-2 judge over-rejected atomicity, fixed by sampling for unanimity-to-reject. The identical mission that surrendered twice now passes and folds — both visible in the browser. **Seventh surviving mutant** caught a vacuous test of mine that matched the CONTRACT's wording, not the verdict, and would have passed a totally blind retry. **AC-0 NOT claimed:** the sixth clause was written then reverted — 32 fixtures legitimately produce one-child splits under the loop's no-gate default, so it is a question about R31's default (`bf62266d`), with the reasoning left in the test file | [history/2026-07-30_054_code-change_gate-a-in-full.md](history/2026-07-30_054_code-change_gate-a-in-full.md) |
| 055 | 2026-07-30 | code-change | **Gate B in full — and the judge that answered with the question.** Gate B ran ONE tier and carried two declared inputs that did nothing: `verificationPlan.depth` was copied into the verdict and ignored (a redundant plan verified once, so a lucky pass WAS a pass), and `redFlags` were collected while the outcome ignored them (a verification-shaped output still passed — exactly the case AC-2 names). Both looked implemented from outside. Mechanical tier deals in FACTS (empty deliverable, effort past ceiling, tools granted but no actions) because a model asked "does this meet the criteria" will sometimes say yes to nothing; the intent tier is a SEPARATE call since letter and spirit pull opposite ways and one call invites reconciling them. Both judges REQUIRED, per R33's lesson. **AC-3 proven at the DB:** raw-SQL amend and withdraw of an issued verdict are rejected, and a correction lands as a NEW verdict with BOTH in the trail — erasing the predecessor would let time travel reconstruct a past that never happened. **Live driving found what no test could:** the intent tier returned the PROMPT'S OWN EXAMPLE PHRASES verbatim as red flags on both attempts (`e7dddf91`) — and since flags now DISCARD work, that would have destroyed good work on nearly every mission. Fixed twice over (prompt must quote the deliverable; tier sampled for unanimity-to-condemn): parroted flags → `redFlags: []`, garbage still failing both tiers. **Two surviving mutants, both real:** a dedup test whose 3-sample arithmetic hid the rule it claimed to prove, and an equivalent mutant that still exposed a flaw of mine — a PASSING verdict could carry a CONDEMNING sample's reason. **ADR-0010** discharges the delegated tier-2 question: keep gemma4:12b, sampling is the mitigation — four consumers now show the same over-assertion, all fixed by sampling not by switching — stating plainly that the full comparative measurement was NOT run. 14 mutants killed, 375 + 85 + 66 green | [history/2026-07-30_055_code-change_gate-b-in-full.md](history/2026-07-30_055_code-change_gate-b-in-full.md) |
| 056 | 2026-07-30 | code-change | **The error class finally picks a rung, and the stall counter stops the rehearsal.** Two declared inputs, neither read where it mattered: `errorClass` was set on every finding and counted by the learning projection while the ladder did `rungIndex += 1` unconditionally — a task SPECIFIED wrong rehearsed the mistake it had just been told about; `stallLimit` was copied parent-to-child and read NOWHERE. `ErrorClassSchema`'s own comment says the class selects the rung. Entry rung is a function of the class; every failure after climbs exactly one — `Math.max` reconciles them and makes the ladder monotonic, else a task cycles between re_decomposition and rung 1 forever. `escalation.ts` owns three testable decisions: `entryRungFor` (falls back to rung 1, NOT the top — a contract granting cheap remedies did not quietly grant expensive ones), `worstClass` (finding ORDER cannot decide the remedy), `isStalled` (same tier/design/failure, most recent run only). **The bounce path had already implemented this rule INLINE**, comment citing R36, while every other escalation site ignored the class — one site knew the requirement and the rest did not. **Five surviving mutants, the worst round yet**, each a real weakness: a fallback test allowing the TOP rung; a stall history ending where "count anywhere" and "count the trailing run" agree; a limit test masked by the length guard; a worst-class test with one class present; and a loop assertion too weak to see the override. **Live on `9123074b`:** entryClass on every escalation, monotonic climb, and `task.stalled` after 3 attempts once the tier capped and attempts became identical. Gate A measured at 34/40 passes — two earlier surrenders were hard objectives, not over-rejection. 8 mutants killed, 400 + 66 green | [history/2026-07-30_056_code-change_entry-rung-and-stall.md](history/2026-07-30_056_code-change_entry-rung-and-stall.md) |
| 057 | 2026-07-31 | code-change | **Delivery with pedigree, the surrender dossier — and the mission that finished without saying so.** Both terminal events existed and were nearly empty: `mission.folded` carried `{childCount, conflicts}`, `mission.surrendered` carried `{reason, blockers}` — an answer with no account of how it was checked, a refusal with no account of what was tried. Both now DERIVED from the trail at handover, never accumulated alongside it. "What it would take" is derived, not speculated: unmet criteria from verdicts, capabilities from `staffing.capability_gap`, budget advice only above 90% of ceiling, the human line only if the ladder reached it — a distractor pins that spending 7 of 20 is NOT told to add budget. **Live driving found the real gap:** the pedigree hung off `mission.folded`, and a KEPT-WHOLE mission never folds — `d042175f` logged "delivered" while its trail simply stopped, no pedigree and no terminal event, which also explains why such missions had nothing marking them finished. `mission.delivered` now fires on both paths exactly once. Verified live three ways: kept-whole delivery with 5 evidence pointers and 4 real assumptions; a surrender with all nine dossier sections; and a RE-ENTRY where the first attempt's findings are pinned into the second contract and inherited by every child. **A limit of our own mitigation, written down:** the plan audit flagged a criterion untestable with a detail reading "The criterion is TESTABLE" — and all three samples agreed, so ADR-0010's unanimity passed it through. Sampling catches an unreliable judge, not a consistently self-contradicting one (`627cd71c`). Stale-sibling footgun bit a third time. 7 mutants killed first pass, 418 + 50 + 66 green | [history/2026-07-31_057_code-change_pedigree-and-dossier.md](history/2026-07-31_057_code-change_pedigree-and-dossier.md) |
| 058 | 2026-07-31 | code-change | **The reviewer starts being measured, and disagrees with itself on the first try.** Every gate is a model asked a question and nothing had ever checked the YARDSTICK — a verdict was issued once and never compared against a second opinion. Three mechanisms that fail differently: calibration catches a reviewer inconsistent with itself; PROBES catch what it cannot, since a reviewer consistently wrong the same way agrees with itself perfectly (why `627cd71c` matters — unanimity sampling is silent for exactly that); independence refuses a verifier sharing lineage with the producer. Two rules exist because the comfortable answer is wrong: an unreviewed verdict is NOT agreement (silence is not agreement), and a re-review from the ORIGINAL reviewer is refused — self-agreement reports a flawless rate precisely when the reviewer is most consistently wrong. Probes score BOTH directions, since over-rejection is the failure our tier-2 judges actually show. **The re-reviewer judges the WORK, not the verdict** — a first `IssuedVerdict` carried only ids, so a second opinion could only be formed from the first, measuring obedience. **Live on `5af8ae4f` it earned its keep immediately:** 1 compared, 1 disagreement, agreement rate 0 — original said `fail`, independent re-review said `pass`. 12 mutants killed, two after strengthening (one unreachable because fixtures passed MORE verdicts than re-reviews; one exposing that a mission with NO verdicts would record an empty measurement reading as a faultless reviewer). **AC-1 and AC-2 NOT claimed:** nothing plants a probe (`2eeef21f`), and verifiers are not staffed entities so there is no staffing attempt to refuse (`bc191e55`) | [history/2026-07-31_058_code-change_reviewer-calibration.md](history/2026-07-31_058_code-change_reviewer-calibration.md) |
| 059 | 2026-07-31 | code-change | **The replay bench — a store that has to resist its own owner.** The fourth memory-fabric store, and the first whose threat model is an INTERNAL component: a benchmark only works while it is honest, and the thing most motivated to corrupt it is the component being scored. **AC-1 is a STRUCTURAL claim and it changed the implementation:** a repository method checking a caller-supplied role is a convention an optimiser can decline to pass — exactly what the criterion rules out — so the seal is a database VIEW. The learning reader is bound to `benchmark_case_open` and cannot name the table; reading a sealed case is a MISSING GRANT, not a forgotten check. A distractor asserts the BINDING, since a behaviour-only test would pass against the role check too. Two refusals are loud on purpose: an explicit sealed request throws rather than returning empty (empty reads as "the sealed bench is empty", a false claim), and a sealed id throws rather than not-found (else the seal is probeable by elimination) — the disambiguation asks the table for a COUNT only, never contents. AC-2 derives staleness from the capabilities missions actually exercise, not a TTL; an EMPTY active set retires NOTHING, since that almost always means the mix could not be determined. Retirement is a tombstone with a reason — a silent removal is indistinguishable from dropping a case you kept failing. **Proven on the REAL database:** migration 0007 applied, open+sealed planted, view shows only the open answer with zero sealed rows, and both constraints reject bad inserts. 6 mutants killed, 103 integration + 438 + 66 green. No UI surface, verified at the DB like R23/R24 | [history/2026-07-31_059_code-change_replay-bench.md](history/2026-07-31_059_code-change_replay-bench.md) |
| 060 | 2026-07-31 | code-change | **The science loop's decisions — built, proven, and honestly not yet running.** R27's four decisions as a pure module; **NO criterion claimed**, because nothing calls them. Mining ranks per CATEGORY across missions (one bad mission is noise), surrender outranks a poor pass rate since stopping is worse than struggling, and a healthy history ranks NOTHING. Experimentation gives every candidate the same budget AND the same cases, and REFUSES an uneven split rather than approximating — a score nobody can trust is worse than none because nobody notices. Adoption needs two bars: replicate (one win is a coin landing well) and hold on a slice never tuned against; absent is not a pass. The DISCARDED result is kept — a rejected candidate is a measurement, and "it failed the held-out slice" says the idea does not transfer, which differs from it being weak. `NEAR_CEILING` reuses R37's 90% rather than inventing a second definition of "expensive". **Two of my own test weaknesses exposed by mutants:** a fixture driving a HEALTHY category and expecting it ranked, and a "surrender is strongest" test whose surrendering category also had a 0/1 pass rate — so it won on compliance and the surrender weight could be deleted. **NOT claimed (`66356a6e`):** grep finds no caller; R27's givens all require the loop to RUN. Fourth time a mechanism has landed ahead of its producer — logged, not glossed. 10 mutants killed, 458 + 66 green | [history/2026-07-31_060_code-change_science-loop.md](history/2026-07-31_060_code-change_science-loop.md) |
| 061 | 2026-07-31 | code-change | **The science loop runs — and R11's projection turns out to be unwired too.** `ScienceLoop` orchestrates R27's four decisions behind three seams, closing HALF of `66356a6e`. Three behaviours the mutants earned: a runner that THROWS counts as a LOSS (a crashed experiment did not succeed, and swallowing it would adopt a candidate that cannot run); the sealed re-check genuinely uses the SEALED cases (a mutant passing the open ones failed three tests — re-running the same exam and calling it held-out defeats R25's split entirely); and replications actually replicate. No sealed case means `heldOut: null`, not a failure — "we could not check" is a different finding from "it did not transfer", and which one happened tells a future hypothesis whether to re-run or abandon. **A find while wiring:** grep for `LearningProjection` outside its module returns NOTHING — R11's projection is also unwired, the FIFTH no-producer occurrence, so the mining half has no upstream at all. **Stopped deliberately:** a live evidence source needs cross-mission history, and `replay` is per-mission; the window should be derived from mission volume rather than guessed at the end of a long session. No R27 AC claimed. 5 mutants killed, 467 + 66 green | [history/2026-07-31_061_code-change_science-runner.md](history/2026-07-31_061_code-change_science-runner.md) |
| 062 | 2026-07-31 | code-change | **The Learning Agent finally reads the ledger — and finds 52 weak spots.** R27 AC-0 satisfied on real data. NO new query needed: `listMissions()` (which backs the fleet rail) folded with per-mission `replay`, emitting one row per (mission, CATEGORY) since that is what the ranker aggregates on — collapsing them would blame every category a mission touched for one category's failures. **No window invented:** the history is every FINISHED mission; a running one has no outcome, and "the last N" would be a constant nobody measured. A distractor asserts a running mission is skipped WITHOUT being read. **An architectural near-miss caught before shipping:** the first attempt added an API endpoint importing worker code, which would have made the control plane depend on the worker — but the Learning Agent is a meta-agent and lives in the worker. Rebuilt worker-side; it only ever APPENDS a ranking (invariant #4). **Proven live:** `learning.weak_spots_ranked` 0 → 1, ranking 52 weak spots from 84 real evidence rows with genuine reasons. **The data volunteered a finding:** most weak spots have `observations: 1` — the planner still invents a category per task despite R38's clustering, so the ranking is dominated by singletons. Mining is correct; the DATA is more fragmented than the ranking assumes. Next step named, not guessed. AC-1/2/3 still need the `CandidateRunner`. 6 mutants killed, 475 + 66 green | [history/2026-07-31_062_code-change_mining-live.md](history/2026-07-31_062_code-change_mining-live.md) |
| 063 | 2026-07-31 | code-change | **The swarm runs its first experiment — R27 COMPLETE.** `BenchCandidateRunner` + `buildScienceLoop` close AC-1/2/3, and R25's sealed slice finally has a consumer. **Judged, not diffed** — the decision worth recording: the recorded outcome is ONE verified answer, not the only correct one, so a candidate saying "100 degrees Celsius" where the case recorded "100C" has NOT regressed; a string comparison would reject a real improvement. The criteria the original verdict was made against are what a re-run is measured against. Everything that is not a demonstrated pass is a LOSS (crashed executor, unavailable judge, unloadable case, empty exam — zero cases would vacuously satisfy "every case passed"). ALL cases must pass, not a majority: fixing one case and breaking another is not an improvement. **Proven live on the real DB and bench:** an improving candidate ADOPTED (2 wins, held-out won); a regressing one REFUSED (0 wins, 2 losses, held-out failed) with evidence recorded either way. A composition file written BECAUSE six mechanisms have landed unreachable — one test checks the executor receives the case's actual CONTRACT, since a store returning empties would still report a clean win. The case store loads via `list`, not per-id, because the Learning Agent's reader refuses a sealed id BY DESIGN and per-id would turn the seal into an exception storm. **Not claimed:** adoption returns a VERDICT, not an enactment (invariant #4), so `parent_design_id` stays unset and `cb939996`/R28 AC-0 remain open. 6 mutants killed, 489 + 66 green | [history/2026-07-31_063_code-change_bench-runner.md](history/2026-07-31_063_code-change_bench-runner.md) |
| 064 | 2026-07-31 | code-change | **The rung that did nothing — `agent_redesign` finally redesigns.** R28's clade score is a recursive CTE over `parent_design_id` that could never fire because NO design had ancestors: `reparent` was called only from its own test, and `upsert` had accepted and persisted the column all along — **it was always writable; nothing ever supplied it.** The producer was in the vocabulary doing nothing: `agent_redesign` is in the real intake ladder and NO site enacted it, so the loop climbed past and re-staffed the same design. Enacting it is where lineage is born: a redesign never reuses an incumbent, its id derives from the parent's (deterministic `designIdForCapability` would otherwise land on the parent's own id and make the lineage a SELF-EDGE), and registration records `parentDesignId`, explicitly null for an origin. **Two mutants worth the round:** a redesign that reuses anyway, and ordinary staffing that ACQUIRES a parent — the second matters because inventing lineage is worse than lacking it, since the clade query would aggregate it as real and a design would inherit a record it never earned. **AC-0 NOT claimed:** the rung sits at ladder index 2 and the live mission climbed only to index 0, so `parent_design_id` went 0 → 0. Proven at unit level, reachable in principle, NOT observed live — `cb939996` records exactly what would close it. 4 mutants killed, 495 + 66 green | [history/2026-07-31_064_code-change_redesign-lineage.md](history/2026-07-31_064_code-change_redesign-lineage.md) |
| 065 | 2026-07-31 | discovery | **Why the redesign rung never fires — two dead routes, found by DRIVING not reading.** Tried to close R28 AC-0 live; `parent_design_id` went 0 → 0 across two engineered missions. **(1)** `budget_exhaustion` is the ONLY class routing to `agent_redesign` and NOTHING emits it — eighth occurrence of a name with no behaviour. **(2)** The indirect route needs three consecutive `execution_error` failures, and mission `240dd00e` was classed a SPECIFICATION FAULT, so R36's entry-rung logic correctly jumped to `re_decomposition` (index 3) and skipped index 2. The ladder is behaving exactly as specified; the redesign path is simply far harder to reach than its position suggests, and nothing had noticed because nothing had tried. **The fix is deliberately NOT applied:** Gate B's mechanical tier already refuses an over-ceiling bundle and could class it `budget_exhaustion` — but `effortSpent` is still a hardcoded 1, so no live task can exceed its ceiling, and reclassing now would create a SECOND route that never fires, which is exactly how the first one came to exist (`e758f460`). No AC closed, but the before/after count is what forced the question — green tests alone would have had me claim AC-0 twice. P28b is BLOCKED with its unblock condition written down | [history/2026-07-31_065_discovery_redesign-unreachable.md](history/2026-07-31_065_discovery_redesign-unreachable.md) |
| 066 | 2026-07-31 | code-change | **One constant, four blocked things — `effortSpent` becomes a measurement.** It was a hardcoded `1`, load-bearing in four places at once: R40's floor only binds for floors >= 2 (intake default is 1), R34's ceiling check could never trip, R37's pedigree reported `spent: 1` per task on every mission, and nothing could exceed its ceiling — so `budget_exhaustion` was unemittable, `agent_redesign` unreachable, `parent_design_id` null, R28 AC-0 unclosable. **Derived, not invented:** the router surfaces no token usage, so the unit is MODEL CALLS — already implied, since `self-critique.ts` adds its own to the same total. Counted per EXECUTION (a shared counter would trip the ceiling on POSITION, not cost); a FAILED call still counts, since the budget bounds what was SPENT, not what worked. **A comment-vs-code disagreement the distractor found:** a throwing assumptions call took the WHOLE execution down, losing an answer already in hand — `AssumptionsSchema`'s comment had always said otherwise. The comment was the requirement. **Live:** all 108 prior `task.executed` rows carry `1`; the next mission carries **2**, pedigree following. Honest about size — 2 against a ceiling of 20 means exhaustion still needs a lower ceiling or repeated escalation. 498 + 66 green | [history/2026-07-31_066_code-change_effort-measured.md](history/2026-07-31_066_code-change_effort-measured.md) |
| 067 | 2026-07-31 | bug-fix | **The `agent_redesign` rung stops lying — lineage is born.** `parent_design_id` moved off ZERO for the first time. Found by driving, not reading: TWO independent faults, either alone fatal. (1) `redesignFrom` read a `let manifest;` declared INSIDE the attempt loop, so it was always the fresh `undefined` → `?? null` → every redesign registered as an ORIGIN; the rung LOOKED enacted while lineage never existed. (2) Gate B raises `budget_exhaustion` when one bundle's `effortSpent` exceeds the ceiling — and the loop accumulates that same figure into `spent`, so the finding IMPLIES `spent >= ceiling` by construction and the pre-attempt guard broke out before staffing, on ANY input. Stepping was no alternative (`maxAttempts: 3` reaches the rung only as the FINAL climb). **ADR-0011:** a budget-exhausted task PRODUCES the replacement (parent = the overspender, `agent.redesigned` recorded) but NEVER RUNS it — ceiling still stops the spend, ladder stops recording a rung it never enacts. Rejected raising `maxAttempts`: inventing a constant to make a mechanism reachable. Also reclassed Gate B's over-ceiling finding to `budget_exhaustion`. **A mutant found a gap no review did:** dropping the `ladder[rungIndex] === 'agent_redesign'` condition survived all 509 tests — nothing asserted the remedy is taken only where the contract GRANTED it. **Live:** `39a621b3` reached the rung and redesigned nothing (before); `6e4c2130` produced `6934528b` with `parent_design_id = 6e25f754` (after), whose clade walk returns `0.452 / 42 observations` for a child with ZERO of its own. AC-0 NOT claimed — `cladeScoreFor` is called by nothing (defect `e4b171c1`). 12 tests, 5/5 mutants killed, 510+66+156+50+26 green | [history/2026-07-31_067_bug-fix_lineage-is-born.md](history/2026-07-31_067_bug-fix_lineage-is-born.md) |
| 068 | 2026-07-31 | code-change | **The clade finally decides something — R28 satisfied.** `bestForCategory` now ranks on the LINEAGE aggregate, not the design's own `clade_score`. `cladeScoreFor` was correct, thoroughly tested, and CALLED BY NOTHING — the ninth name-with-no-behaviour; AC-0 asks that the DECISION use a clade score, and a query nobody calls considers nothing. **ADR-0012:** inherited at FULL WEIGHT, no generation decay (a decay factor would be invented; the observation weighting already IS the discount, and it's derived — a child's one run barely moves a parent's thirty-run mean). Evidence bar now counts LINEAGE observations, which is what makes a redesign biddable at all. Ties break on OWN observations: child and parent share one lineage and score identically by construction, so without it an unrun redesign would evict the incumbent it was derived from. **Three of my own tests passed before any fix, for the WRONG REASON** — the ancestor was still active and won on its own column, so the heir's lineage was never consulted; retiring it isolates the mechanism. **Mutation lesson: two redundant guards masked each other** — dropping `d.active = true` from the CTE anchor survived all 109 tests, dropping it from the outer WHERE also survived; only removing BOTH killed 4. **Live, both directions:** old rule saw 1 candidate for `mission`; new rule sees 2 — `6934528b` ranked at clade 0.4524 over 42 INHERITED observations with ZERO of its own. Mission `a4696805` ran through it end to end and delivered. Limitation stated not hidden: a redesign's first run is taken on inherited credit. 6 tests, 6 mutants (4 killed, 2 equivalent), 109 integration + 510 + 156 + 66 + 50 + 26 green | [history/2026-07-31_068_code-change_clade-decides.md](history/2026-07-31_068_code-change_clade-decides.md) |
| 069 | 2026-07-31 | code-change | **The fast loop's core, guard and store — bounds derived, reach bounded three times.** R26's `fast-loop.ts`, `checkFastLoopReach` and migration 0008 (`hot_fix`) built and proven; ADR-0013. **Four bounds, none invented:** "repeatedly" is the contract's own `stallLimit` (R36 already asks that question about attempts); the window is the baseline's own evidence count; the prediction is the rate the category achieves on its OTHER criteria — pooled not averaged, so one observation can't outvote thirty (R28's argument) — degrading to "strictly better" WITH a `basis` field when there are no peers; and NO significance threshold, since AC-1 says "does not move". **A hole the tests found:** a window closing only by FILLING never closes once the category stops appearing, so the fix would outlive its mission — it now also closes on mission end, and under-filled reverts. **Revert bar is the BASELINE, not the prediction** — reverting a real improvement isn't what AC-1 asks; the shortfall becomes an R27 hypothesis instead. **Reach bounded THREE times** (type · allow-list guard · CHECK constraints) because "by construction" rules out anything a call site must remember. **Two masking failures, both found by mutants:** a blocklist guard survived all 27 tests because the `kind` check fired first; nullable `previous_value` survived all 127 because every fixture supplied one. **A NUL byte** entered the source via a bash heredoc — third heredoc corruption here. 22 mutants, 22 killed. 537 + 128 integration + 156 + 66 + 50 + 26 green. **R26 NOT satisfied — nothing calls any of it**; logged as defect `188c6892` at the moment of creation so the gate can see it | [history/2026-07-31_069_code-change_fast-loop-core.md](history/2026-07-31_069_code-change_fast-loop-core.md) |
| 070 | 2026-07-31 | code-change | **The fast loop wired — R26 satisfied, and it really reverted.** The producer was the whole gap: the loop now collects a `GateBOutcome` per criterion and calls `detectHotSpot` with the contract's own `stallLimit`, `buildWorkerSeams` builds the seam, and `WorkerDependencies.hotFixes` is REQUIRED so the deployed binary cannot run without it (the `commons` pattern, which exists because the registry was a null-bidding stub for the project's whole life). **`await runFastLoop(true)` sits BEFORE the surrender path returns** — attached to only the delivered path it would have missed half the missions, R37's pedigree shape. **Ordering chosen for what a crash leaves behind:** log-then-patch on apply, restore-then-record on resolve. **Not routed through the ratchet** — that governs PERMANENCE, and a hot-fix is impermanent by construction; making it earn permanence would make it the slow loop (R23). **Live, mission `90f2387f`:** two Gate B failures on `m-1` → fired at stallLimit 2 → worker-layer patch with `basis: peer_criteria` (a real peer, `m-2`, passed) → the patched attempt bounced → window closed with NO observations → auto-reverted on mission end. Real `hot_fix` row `reverted/1.000/null`; real `agent_design` back to 88 chars with the patch text gone — **the ASSET was restored, not merely the log**. Shaping the trigger took FOUR live missions (a SHA-256 criterion passed because the judge can't verify hashes either; a DOI criterion bounced as ambiguous; a 3-term split was refused twice by Gate A). **Honest split on AC-1:** live took the no-observations default; the literal "measured rate does not move" clause is proven by the loop test against the real composition, with the reason string PINNED so it cannot drift. A mojibake turned out to be my terminal, not the ledger — no defect logged. 21 new tests; a mutant hard-coding `outcome='kept'` survived 134 integration tests and is now killed. 552 + 135 integration + 156 + 66 + 50 + 26 green | [history/2026-07-31_070_code-change_fast-loop-wired.md](history/2026-07-31_070_code-change_fast-loop-wired.md) |
| 071 | 2026-07-31 | code-change | **The verifier becomes a staffed entity — R35 AC-2, refused live.** Gate B's judge was a bare model call with `reviewerId` = the mission id, so "a verifier never shares design lineage with what it verifies" had nothing to rule on. Verifiers are now staffed designs in their OWN market (`verification.<capability>` — sharing the producer's would hand the same design to both and make a violation the normal case). New query `ancestorsOf` returns member IDs where `cladeScoreFor` returns a weighted mean — checked it was not a duplicate before adding. **A mutant caught what review didn't:** registering the fresh verifier with `parentDesignId: producerDesignId` passed all 561 tests, recording the exact lineage the function exists to rule out — self-inflicted and delayed, since the next staffing would refuse the verifier it just created. **And the LIVE SYSTEM caught what the mutants didn't:** planting shared lineage produced `verifier.unstaffed` rather than a replacement, because `designIdForCapability` is deterministic and the "fresh" design derived the SAME id as the bid just refused; the existing test passed only because its two strings differed by accident. Fixed by deriving from the producer on refusal, mirroring R28's `#redesign-of-`. A second correction went to the test itself — a `deriveDesignId` stub returning a constant could never go green. **Live:** `f64308b1` staffed a distinct verifier; `1139beb8` fired the refusal and exposed the collision; `2439fba8` refused bid `2a3f2861` with its reason and staffed independent `7a983cfe`. The planted lineage was REVERTED afterwards so no fabricated edge feeds later clade scores. 17 tests, 6 mutants. 568 + 139 integration + 156 + 66 + 50 + 26 green. **AC-1 still open** (`2eeef21f`) — nothing plants a probe | [history/2026-07-31_071_code-change_verifier-independence.md](history/2026-07-31_071_code-change_verifier-independence.md) |
| 072 | 2026-07-31 | code-change | **Rubber-stamping is measured — R35 satisfied, and the known-bad probe was CAUGHT.** `probeMisses` had scored both directions since P35 and `probes()` had been read by the loop the whole time; nothing implemented it. This dead mechanism mattered more than most: ADR-0010's unanimity sampling cannot catch a judge wrong the same way every time (`627cd71c`), and only a KNOWN ANSWER can. **Construction:** a known-bad probe pairs one sealed case's contract with ANOTHER case's verified answer — fabricates nothing, and unlike an EMPTY deliverable (refused by the mechanical tier with no model) it can only be caught by reading against the criteria, which is the tier that rubber-stamps. The planter REFUSES rather than inventing: one case has nothing to borrow; identical answers would label a correct deliverable "fail" and score the reviewer a miss for being right. Known-GOOD probes too (58% false-bounce rate). **Probes are RUN, not declared** — the old seam returned `{taskId, expected}`, which could never measure anything since no verdict for a synthetic task exists. **A fixture that was relabelling, not planting:** it returned the MISSION's own task id and passed for two iterations only because nothing implemented `probes`. **Live (`e8077776`):** planted 4, processed 2, `probeResults` shows `{expected: fail, actual: fail}` — caught — and zero misses that are demonstrably zero. **Two bounds recorded not assumed away:** the live reviewer caught it so no miss fired (the miss clause is pinned by the fixture test against the real composition); and 2 of 4 probes were unevaluable because a sealed case holds `{"o": "sealed case"}` and the bench has NO live producer (defect `c1b3ae71`) — deliberately NOT deleted, since removing inconvenient bench cases is the first step to owning the benchmark. 14 tests, 6 mutants (5 killed, 1 equivalent). 582 + 139 integration + 156 + 66 + 50 + 26 green | [history/2026-07-31_072_code-change_probes-planted.md](history/2026-07-31_072_code-change_probes-planted.md) |
| 073 | 2026-07-31 | code-change | **Gate A's sixth clause, built on the footing the revert taught.** "Sane use of the decompose-or-delegate gate" is built, wired and live; R33 satisfied, P33 completed, `bf62266d` resolved. The check was never in doubt — a "split" producing exactly ONE child is the gate's decision made incoherently — but applied unconditionally it broke 53 fixtures, because the loop's no-gate default IS "always split" and the clause was faulting the planner for the loop's own default. **The fix is not where it looked:** rather than change R31's default (a separate question the defect was explicit about), the clause is scoped to plans where a gate ACTUALLY DECIDED. `decomposition.decided` recorded WHAT was decided on both paths but never WHO — distinguishing them by matching rationale prose would be a convention; `decidedBy` is a fact. Both directions audited (one-child split; split despite keep-whole), and a gate that THREW records as `default` since an outage is not a decision. **Two mutants survived the pure function's own tests** — the loop never passing the decision at all, and a thrown gate counted as deciding — invisible from a pure function's tests because they cannot see whether anything calls it; `gate-a-composition.test.ts` drives the real `runMission` and kills both. Reverting to the unconditional clause now fails 53 tests, so the revert reason is a guard not a memory. **Live (`7cc3ad72`):** `decidedBy: gate` reaches production and Gate A passed a sane two-child split; the FAULT direction is pinned by composition tests, since a live one-child gate split would mean controlling the planner. 11 tests, 7/7 mutants. 593 + 156 + 66 + 50 + 26 green | [history/2026-07-31_073_code-change_gate-a-sixth-clause.md](history/2026-07-31_073_code-change_gate-a-sixth-clause.md) |
| 074 | 2026-07-31 | code-change | **Decomposition templates learn and are reused — R31 satisfied.** Templates are distilled from splits that survive Gate A, offered back as planner guidance, and scored on what they guide; ADR-0014. **A defect found on the way** (`2e79255f`): `createStepwisePlanner` — the planner `runtime.ts` actually wires — destructured `{ contract }` and never read `rejectedBecause`, so EVERY re-split in the deployed system re-proposed from the bare objective and rehearsed the rejection it was handed. Every loop fixture honoured the seam; production did not, and a seam's tests cannot see whether the real implementation reads its inputs. **Templates are their OWN store, a defended deviation:** "in the Asset Registry" points literally at `agent_design`, but R26's fast loop patches `role_instructions` there (a recipe could be silently rewritten mid-mission), `bestForCategory` would bid templates as agents, and the two are scored on different things. So the registry's PROPERTIES without its machinery. **Derived choices:** keyed by capability with a unique constraint (two templates per kind of work fragment the evidence they exist to gather); `minObservations` 0 not 3, because nothing else offers templates so a bar of 3 would be its own blocker; `remember()` does not overwrite, since the incumbent carries the evidence. **The producer:** a surviving split with no template distils into one, using the objectives that PASSED as the recipe; a rejected split teaches nothing. **Live, both halves:** `3c0923dc` learned a template (observations 0, score null — unproven); `d8e07ce4` used it and moved the row to observations 1, score 1.0. 27 tests, 7/7 store mutants. 607 + 150 integration + 156 + 66 + 50 + 26 green. Limitation inherited: both keyed to capability `mission` because the taxonomy has not converged | [history/2026-07-31_074_code-change_decomposition-templates.md](history/2026-07-31_074_code-change_decomposition-templates.md) |
| 075 | 2026-07-31 | code-change | **The learning observatory reads events that exist — R19 satisfied, PHASES PILLAR GREEN (all 51).** `buildLearningView` bucketed four event types NONE of which anything emits — no producer in worker or API, zero rows in the live ledger. It rendered four empty lists and would have forever; its own empty-state blamed "R26/R27 are not built yet", stale since both loops shipped. **Rewritten IN PLACE** — the buckets were right, the sources were not: now `fast_loop.hot_fix_applied` (carrying `bounds` + `predictedEffect`, declared BEFORE any result, which is what makes them PRE-REGISTERED), `fast_loop.hot_fix_resolved` (the ratchet), `learning.proposal_emitted`, `decomposition.template_learned`. **Live in Chrome, all four lenses:** workforce (3 staffed, design+version, tier, runtime, compliance), timeline (2 swimlanes, waited/ran, ordered events), ledger explorer (16 of 16, filters), observatory (the REAL revert from `90f2387f`, the REAL template from `3c0923dc`). **The in-flight clause reached by TIME TRAVEL** — an experiment opens and closes inside one mission, so two attempts to catch it live failed; scrubbing back to a moment between apply and resolve rendered "predicted 0.5 vs baseline 1 over 2 observations (peer_criteria)". That works because AC-4 makes every lens a projection of the same event list. **Fourteenth dead mechanism found** (`d08191c8`): `ProposalEmitter`, invariant #4's only outward channel, is defined, tested and NEVER CONSTRUCTED — deliberately left to R29 rather than inventing a trigger. Bound stated: the petitions clause is proven as a RENDERING, but the populated case was never clicked through because the system cannot produce a petition. 6 tests rewritten RED-first, 66 dashboard green | [history/2026-07-31_075_code-change_learning-observatory.md](history/2026-07-31_075_code-change_learning-observatory.md) |
| 076 | 2026-07-31 | bug-fix | **A judge that argues against its own verdict is now discarded.** Gate A discards a testability finding whose own rationale asserts the criterion IS testable — defect `627cd71c`, fixed with the remedy it named when it was logged. Live mission `02a7d050` flagged a criterion untestable with the detail "The criterion is TESTABLE"; same shape as `f720938a` and `890cdea5`. **Why the existing mitigations are silent:** ADR-0010's unanimity needs all samples to agree before rejecting, and all three flagged — sampling catches an UNRELIABLE judge, not a CONSISTENT one. **And probes do NOT cover it**, though they look adjacent: probes measure calibration AFTER a mission, this kills one at PLANNING time. **The check** is deterministic and narrow — an UNQUALIFIED assertion of testability, SENTENCE-scoped (a negation elsewhere must not excuse it) with a qualifier family protecting the useful form ("testable only if…", "would be testable if…" are the judge explaining what would FIX it). Restricted to testability, since firing on "is clear" would misread the clarity judge's vocabulary. **A stale test found on the way:** `mission-control.spec.ts` required the observatory to say "not built yet" — true when written, false since R26/R27 shipped; the property is still asserted, the obsolete reason is now asserted GONE. 11 tests (8 predicate + 3 driving the real `gateA`), 6/6 mutants killed. 618 + 160 + 66 + 50 + 26 green. Bound: fixes the TESTABILITY clause only — the general form needs a model, which is what was wrong | [history/2026-07-31_076_bug-fix_judge-self-consistency.md](history/2026-07-31_076_bug-fix_judge-self-consistency.md) |
| 077 | 2026-07-31 | code-change | **Invariant #6 is enforced by the running system, not by its tests.** The Context Broker is constructed, used and logging; `488709be` and `753bc6dd` resolved. **Two things were missing and only one was obvious:** the broker had no constructor (the logged defect), but wiring it alone would have changed nothing because `authorContracts` gave every child `entitlements: []` — a wired broker would have denied every request and the invariant would have been "enforced" by having nothing to enforce. Each child may now request `commons:<its own capability>` — scoped, not blanket. **The GRANT ID is the evidence, not the source:** `consulted` carries `viaBrokerGrantId` (a field that has existed since R40 for exactly this); a source without it is indistinguishable from a direct read. **Live (`680adb06`):** `context.granted` with the grant id landing on `task.executed`'s `consulted`. **A new defect logged SEPARATELY rather than folded in** (`913ead75`): the commons holds 22 quarantined and ZERO published because nothing calls `corroborate` or `publish` — the consumer is live and correct and always serves an empty list. Deliberately not fixed by loosening the filter, which would hand workers the unproven material quarantine exists to hold back. **A parse error worth remembering:** `packages/*/src` inside a block comment contains `*/` and closed it early. 9 tests, 6/6 mutants; `WorkerDependencies.knowledge` REQUIRED — the pattern that has caught five dead mechanisms. 627 + 160 + 66 + 50 + 26 green | [history/2026-07-31_077_code-change_context-broker-wired.md](history/2026-07-31_077_code-change_context-broker-wired.md) |

