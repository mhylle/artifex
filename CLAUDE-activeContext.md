# Active Context

> Current state, goals, and next-phase options. Update at the end of any significant session. This file is the authoritative "where we are".

**Last updated:** 2026-07-31

## Where We Are

> **Everything below the "As of 2026-07-30" marker is HISTORY, not current state.** It describes the v0 build (P0–P13, R1–R13) and was written when that was the whole project. It is kept because the reasoning is still worth reading, but **do not read its status claims as current** — in particular it says "THE PROJECT IS DONE", which was true against the v0 scope and is not true now. `CLAUDE-history.md` is the index of what happened since.

### Current state — 2026-07-31

- **Scope grew far beyond v0.** The frozen dossier's operator console and the system's missing spine were deferred in *prose*, never as requirements — process defect `cd18baa0`. They are now **R14–R41 in Tasktracker**, and the goal is written down in `docs/GOAL-full-system.md`: every requirement satisfied, `getProjectDoneness` green, and **every capability exercised through the real UI in a real browser via Playwright** — because "the logic was proven by a harness" has already fooled this project twice.
- **All 41 requirements read `satisfied`.** R30 (intake dialogue) was the last, closed 2026-07-31 when AC-2 was proven live on mission `63498d62`: five low-stakes assumptions carried from intake, `task.executed`, three `assumption.became_load_bearing` escalations, then `mission.delivered` — escalated at that moment, strictly before delivery.
- **Doneness: 3 of 5 pillars green.** Phases (all 56), acceptance criteria, and completed-phase integrity pass. Blocking: **1 open defect (`dd2e9d18`, low — partially fixed)** and **6 architecture-drift entries, all `orphaned`** (a walker limitation, friction `7ef7a267`; `missing` and `stale` are both 0 and must stay 0). **Never make the drift pillar green by deleting truthful code references — that is `cd18baa0` repeating.**
- **The intake flow is the way in.** Every mission now blocks at intake with genuine questions — measured, one high-stakes question appears in 8 of 8 trials regardless of how well-specified the request is, and *more* specificity produces *more* questions. To reach any deeper path, answer it: `POST /missions/:id/control {"taskId":"<missionId>","action":"decide","decision":"approve","note":"<answer>"}` re-enqueues the mission and the worker skips intake and runs (ADR-0023).
- **ADRs 0010–0024** now carry the decisions taken since v0. The most load-bearing recent ones: **0022** (the intake dialogue's judged half runs in the worker, not the control plane), **0023** (the intake block honours `prior.decided` — the rule the escalation ladder already used), **0024** (a mission's status is its *last* terminal event, and both `mission.folded` and `mission.delivered` mean delivered).
- **Open, and deliberately not guessed at:** 26 missions show as "running" with no terminal event — dead runs from crashed workers, oldest ~19 hours, zero actually in flight. **Nothing writes an event when a worker dies**, so the ledger cannot tell "running" from "abandoned". The candidate is a startup sweep appending a corrective `mission.abandoned`; it needs its own ADR. A staleness threshold was rejected as a hardcoded constant standing in for a measurement.
- **Also open:** the cockpit offers a `Reject` button, and `foldPriorTrail` clears an escalation on any `operator.decided` regardless of the decision value. Logged as an open question rather than a defect, because the requirement text does not settle which reading is right — and the fold feeds the escalation ladder too, so changing it there would silently change every rung.

### As of 2026-07-30 — the v0 build (history)

- **Stage:** Design complete + v0 plan `ready` + **P0 · P1 · P2 · P2.5 · P3 · P3.5 · P4 · P5 · P6 · P7 built & verified** (P0/P1 2026-07-24, P2 2026-07-25, P2.5/P3 2026-07-26, P3.5 2026-07-27, P4–P7 2026-07-30). Security/perf still deferred. **✅ THE PROJECT IS DONE — all five doneness pillars pass** (phases · acceptance criteria · defects · completed-phase integrity · architecture drift). 258 unit tests + 31 integration, green across six workspaces.
- **All 5 defects FIXED (2026-07-30), each TDD'd RED-first and mutation-verified:** `cd677737` reflection self-consistency guard (discard a revision that breaks a criterion the critique itself marked met — invents no threshold); `626f6596` transient retry before an escalation rung is spent (the ladder is for *substantive* failure; a hiccup shouldn't burn a real remedy, and the risk compounds with fan-out); `d678cd8c` admission samples N times and carries the **pass rate as data** (unanimity to admit, rate for the Tier Policy engine — no invented threshold); `8a6ee598` `readSinceCommitted` reads below the **xmin commit horizon** (reproduced the skip with two real transactions first; deliberately not a "lag N seconds" window, which would turn correctness into tuning); `8b7e9e95` resolved as mitigated (stepwise flat-schema planner + output bound + loop resilience, verified in P13).
- **One honest caveat on the ACs:** R3 AC-0 still reads "Qwen2.5" while the models moved to qwen3.5/gemma4 in P3. Accepted on **substance** (tier 1 does resolve to the catalogued Ollama entry with its declared params) rather than rewriting the AC to match what was built.
- **P13 complete (phase `8b133a67-…`, R9) — ARTIFEX WORKS END TO END, 20/20.** intake → Redis queue → mission loop on **real local models** → real Postgres ledger → cockpit task tree, **DELIVERED** with a coherent briefing. 12 events replayed with strictly ascending `seq`, human intake first, Gate A provably before execution. **Smallest-model-first and BOTH tier directions proven in the same run**: leaf → `qwen3.5:2b`, high-blast irreversible root → tier 3 (upgrade), proven clade 2→1 (downgrade). Shipped **`createStepwisePlanner`** first (asks the subtask *count*, then ONE subtask at a time against a flat schema) to mitigate defect `8b7e9e95` — removing the cliff rather than raising the guard rail.
- **⚠ OPEN DEFECT `626f6596` (medium) — end-to-end success is STOCHASTIC.** Run 1 of the identical code **surrendered at 18/20** when a leaf's model call threw three times. Every mechanism degraded correctly (caught not crashed · one rung per failure · complete ordered trail · surrender with blockers · cockpit honest) — that run is evidence *for* the design. But with `n` leaves each needing a model call, **failure probability grows with fan-out**, the direction this system is built to scale in. Distinct from `8b7e9e95`: the planner never failed, and run 1's failures were on the **flat** `{answer}` schema — **flattening was necessary but not sufficient.** Settle before wide fan-out.
- **P12 complete (phase `04557902-…`, R10 AC-2 dashboard half) — THE FRONTEND GAP IS CLOSED.** Angular **Mission Control**: the task tree is a **pure function of the event list** (`buildMissionTree`) — no store, no mutation, no accumulated view state — which is the strongest way to hold "a view, never a second truth" (invariant #1). **Status is derived, never stored** (a task is `verified` because its last Gate B verdict passed), so the cockpit cannot disagree with the ledger even transiently. Events sorted by **`seq`** because a websocket delivers promptly but not in order. A partial trail renders a *partial* tree, not a wrong one. `LedgerFeed` holds the raw trail and nothing else, de-duplicated by `seq` on reconnect. 16 dashboard tests (was 2), **246 repo-wide**; live-verified serving HTTP 200. **Angular dashboard + NestJS control plane + agent-runtime worker now all exist and are wired to the same ledger.**
- **⚠ Testing lesson repeated in P12:** the first mutation (caching the tree) **changed nothing**, so the removal distractor was vacuous. Re-running with a mutation that modelled the *real* failure — a cockpit **accumulating** its task list — made it bite. **A mutation must model the actual failure mode, not merely be a change.**
- **P11 complete (phase `808ff1cf-…`, R11):** the **Learning seam**. Both ACs are about what the learner *cannot* do, so both are structural. The **projection narrows whatever it is handed to a single `replay` capability** — even given a full repository, no write path is retained — and read-only is proven **behaviourally** (hand it an object that HAS `append`, assert it is never called; a type annotation is a promise, an unused capability is evidence). **`CONSTITUTIONAL_CORE` is `Object.freeze`d, not commented** — a learner that could edit the yardstick would make every measurement unfalsifiable. **A proposal may TARGET the constitution** (that is the amendment protocol working — arguing is allowed) but there is deliberately **no `apply`/`amend`/`adopt`**; the distinction between arguing and acting is the design. 126 worker tests, **234 repo-wide**; mutation-verified. Dogfooded on the real ledger: projection given the real repository left the count unchanged; the proposal landed as a `learning` event with `status=proposed`/`appliedBy=null` while the constitution stayed byte-identical.
- **P10 complete (phase `3c09a3d6-…`, R10 AC-1; AC-2's *dashboard* half is P12):** the **NestJS control plane**. **Intake refuses rather than defaults** (no success criteria → rejected; inventing one would be the control plane deciding what success means). Task zero is **validated before anything is recorded or enqueued**. The human intake is a **first-class ledger event** (symmetry rule). The live stream **hydrates by `seq`** rather than trusting the NOTIFY payload — P2 publishes a *pointer* because NOTIFY caps at 8000 bytes, and hydrating is the same path used to catch up after a disconnect. `replayThenSubscribe` means a cockpit opened mid-mission sees the whole trail. **There is deliberately no "run" endpoint** — the API enqueues and stops. 17 api tests (was 1), **224 repo-wide**; mutation-verified. Dogfooded on real Postgres + Redis/BullMQ + real NOTIFY, including a BullMQ Worker picking the mission off the queue.
- **Tooling: `packages/api` moved from jest → vitest.** Jest's CJS default can't parse the ESM-only workspace packages without `moduleNameMapper`/`transformIgnorePatterns` workarounds; every other package already uses vitest. Needed **`unplugin-swc`** because **NestJS DI reads `emitDecoratorMetadata` and vitest's esbuild does not emit it** — without it every DI-constructed provider resolves to `undefined` and it looks like a broken test, not a missing transform.
- **P9 complete (phase `9d86094c-…`, R9) — ARTIFEX RUNS END TO END.** `runMission` assembles P4–P8.6: decompose → Gate A → staff → execute → Gate B → fold-up. **Live with real local models: DELIVERED in 78s**, 11 events replayed from real Postgres with strictly ascending `seq`, Gate A provably before any execution. Three guarantees: Gate A precedes execution; **one failure climbs exactly one rung** (only `retry_higher_tier` moves the tier — other rungs change *who* runs, not how much model); **surrender is first-class**, carrying blockers. **Hardened mid-phase: a throwing seam climbs the ladder or surrenders with the error, never kills the mission** (a crash loses the whole ledger trail). 116 worker tests, **208 repo-wide**; mutation-verified.
- **Two bugs the live run found were MINE, not the loop's** — worth remembering: (1) the worker was prompted with `objective` only and **never shown its acceptance criteria**, so Gate B correctly failed work the planner had specced differently (invariant #2: the contract *is* the spec); (2) the planner ran at tier 1, but **ADR-0002 puts root decomposition at the top of the ladder** — a bad split is inherited by every descendant. Fixing that took the mission from surrendered to delivered.
- **⚠ OPEN HIGH DEFECT `8b7e9e95` — models run away on complex nested schemas**, emitting chain-of-thought *inside the JSON channel* until the context limit (32,690 tokens on a 78-token prompt). Established by experiment: simple schemas fine on both models; **`/no_think` makes it WORSE**; stochastic. Mitigated (`DEFAULT_MAX_OUTPUT_TOKENS` = fast failure instead of expensive crash; loop survives it) but **not fixed**. The real constraint is **schema complexity per tier**, which `taskClass` cannot express. **Settle before P13.**
- **P8.6 complete (phase `6cd1cae1-…`, R12 runtime):** the **self-critique pass**. **R12 complete — with P8.5, both ADR-0006 gaps (reflection + tool use) are now closed.** Everything shaped by *self-review must never become self-verification*: the record has no `gate`/`outcome`/`verdictId` by construction; **`gateBRequired` is an unconditional literal `true`** (no branch can short-circuit review, and a test exists so a future change must fight it); the pass validates it was handed a `WorkerContractView`; the event is filed under **`execution`, never `verification`**. Invented-criteria refusal, same as the Reviewer. Reflection spends the contract's existing budget, attributed not capped. 99 worker tests, **189 repo-wide**; mutation-verified.
- **⚠ OPEN HIGH DEFECT `cd677737` — reflection can REGRESS a deliverable.** Dogfood: the model diagnosed correctly (`ac-2` unmet, no citation) then **repaired destructively** — `"22% in 2024"` → `"5% in [Source Name]"`, corrupting a correct figure and breaking `ac-1`, which its own critique had just marked met. **This inverts R12's economic justification**: a pass that regresses spends budget to make work worse *and* still pays for the Gate B rejection. Unconditional Gate B is what stops it shipping — expensive, not unsafe. Hypothesis (one sample): **diagnosis and repair are different capabilities**, and running both at the worker's tier assumes they are the same job. Three candidate fixes on the defect. **Settle before P13.**
- **P8.5 complete (phase `ede231c1-…`, R13 runtime):** the **Action Broker** — mediated, entitlement-scoped, fully logged tool use. **With P2.5's contract surface, R13 is COMPLETE: Artifex agents can now act, not only think.** **One door** — every invocation funnels through `invoke()` (successes, failures *and* refusals), which is what lets sandboxing land later without redesign. **Three denial paths, each typed AND logged**: silently ignoring hides a capability gap the Learning Agent should see; silently permitting defeats the entitlement. The write-class test descriptor **throws if reached**, so a silent permission surfaces as the wrong error rather than a pass. `blastRadius` bounds the reachable tool set (a declared blast radius must *cover* the tools used) and the autonomy dial gates ratification — both ADR-0007 tables as stated policy functions. A throwing tool records `outcome:'error'` rather than being swallowed. 87 worker tests, **177 repo-wide**; mutation-verified. **Dogfooded with a REAL tool doing REAL I/O** (a search over the Asset Registry in Postgres, agent code never touching the pool): 2 invocations + 3 denials replayed from the real ledger.
- **⚠ Defect `8a6ee598` (ledger `seq` gaps under concurrent writers) is now REACHABLE** — P8.5 introduced the first concurrent appenders, exactly as its phase body warned. Still tracked separately; deliberately not papered over inside the broker.
- **P8 complete (phase `2c1c8e8e-…`, R8):** **Context Broker** + **BrokeredFabric** + specialist restate-or-bounce. **The grant IS the capability** — `read()` requires a grant the broker issued *for that exact source*, so bypassing the broker is a missing argument that fails at the call site, not a rule anyone can forget. **Denials are logged too** (a silent refusal leaves no more trail than a silent permission). Entitlement is checked against **this contract**, never a global allowlist. The specialist **validates it was handed a `WorkerContractView`**, so P2.5's withholding is honoured rather than assumed. **Bouncing does no work** — "bounce but try anyway" is guessing with a disclaimer. 72 worker tests, **162 repo-wide**; mutation-verified. Caught a smell in my own API: an *optional* fabric made a working broker and a useless one indistinguishable — now mandatory.
- **⚠ Finding that contradicted my prediction (logged): BIGGER MODELS OVER-BOUNCE.** Same clear/vague contract pair, three tiers: `qwen3.5:2b` **DELIVER/BOUNCE (correct)**, `qwen3.5:9b` BOUNCE/BOUNCE, `gemma4:12b` BOUNCE(3)/BOUNCE(3). Larger models find *more* ambiguity, and on a gate whose failure mode is "refuse to start" that is false bounces — a swarm wired at Tier 2 here would stall on executable work. **"Smallest model possible" is not purely a cost argument.** It also shows `taskClass` is too coarse: fold-up and the clarity gate are both `evaluative` yet want opposite directions. Bounce rule left as-is (a severity threshold would be an invented cap); prompt calibration is P11's remit.
- **P7 complete (phase `5b302a75-…`, R7):** both **verification gates**. The judgement is delegated to a judge seam; the **bookkeeping is not** — a judge is an LLM, and one asked to assess five criteria will sometimes return four. Both gates iterate the **contract’s** criteria, never the judge’s answer, so an **unassessed criterion is a FAILURE, never an implicit pass** (the gate would otherwise stop gating while still reporting success), and a judge reporting criteria the contract never had is **rejected** as grading a different task. Uncovered → **`specification_fault`**, so the ladder re-decomposes instead of rehearsing the same mistake. Gate B records the depth the **contract** demanded. **Immutability is a property of the ledger, not the object** — proven by appending a verdict then having raw-SQL edit and delete both rejected. 59 worker tests, **149 repo-wide**; mutation-verified. Dogfooded on **gemma4:12b** — gate work resolved to tier 2, exercising P5’s per-seam tier convention rather than just documenting it.
- **P6 complete (phase `60a9d124-…`, R6):** **Agent Creator** (reuse-first staffing) + the **Asset Registry** store in `memory-fabric` (migration 0002). The load-bearing idea is the **evidence bar**: `bestForCategory` needs ≥3 observations before a clade score counts — below it a design is *unproven*, not bad, and treating unproven as proven is how one lucky run becomes a permanent default. Tier is **computed, never chosen by the designer** (an agent picking its own model is the budget equivalent of grading its own homework). The **validation harness is built from the contract's own acceptance criteria**, one check per `criterionId` — a generic harness would promote designs on a test that never varied. Manifests can never entitle wider context than the contract grants. Clade score is a **running mean carried with its observation count**; losers are **deactivated, never deleted** (invariant #5). 46 worker tests, **136 repo-wide**, **29 integration**. **Dogfooded the full ratchet on real models:** no-bid → `gemma4:12b`; 1 outcome → still no-bid; 3 outcomes → bids (clade 0.93); reuse → **tier 2→1 → `qwen3.5:2b`**; deactivate → stops bidding, evidence survives.
- **⚠ Testing lesson from P6 (now a GLOBAL learning): two of my own distractors were VACUOUS.** Written at `medium`/`generative` where the proposed tier already equals the floor, so the clade-discount branch was unreachable and both sides returned 1 regardless of the code; one also used `toBeLessThanOrEqual`, which passes on equality. The mutation check changing *nothing* is what exposed them. **For any test guarding a conditional discount/penalty, verify the scenario actually reaches the branch.**
- **P5 complete (phase `9f4eb512-…`, R5):** the **Orchestrator**. `decompose()` authors full contracts and *refuses* three classes of proposal — ungradeable (no acceptance criteria), overlapping (no anti-scope), unaffordable (effort shares > 1) — because by execution time the contract is the only spec that exists. **Budget is DIVIDED by effort share, never copied** (copying multiplies spend by the fan-out). Children inherit what they must not choose: `autonomyDial`, `escalationPolicy`, `verificationPlan`. `foldUp()` **requires** a reconciler — children are kept ignorant of each other (invariant #6), so nothing else can notice two siblings contradicting each other; the distractor asserts child answers do NOT all survive verbatim. Model-backed `createModelPlanner`/`createModelReconciler` with **worker-local** TypeBox schemas (nothing outside the Orchestrator sees a proposal; `shared-types` is for cross-boundary shapes only). 35 worker tests, **125 repo-wide**; mutation-verified. **Dogfooded on the smallest model** (tier 1 → `qwen3.5:2b`): 3 clean subtasks in 36s, all valid contracts, budget 22.5 ≤ 30, and fold-up **detected a planted contradiction**.
- **Convention for P9 (learned in P5): compute the tier PER SEAM, not per task.** Decomposition is `generative`; fold-up and Gate B are `evaluative`, which the P4 policy already floats a rung higher. At 2B the reconciler caught the conflict but its prose was near-incoherent — "smallest model" is per-seam.
- **P4 complete (phase `b2cdf558-…`, R4):** **Tier Policy engine + Constitution guards** in `packages/worker`. The engine is **floor-first and biased downward** — cheapest tier the risk permits, climbing only when justified (invariant #7; start-high-discount-later is how a swarm becomes unable to afford fan-out). **The floor is constitutional**: blast radius sets the base, irreversibility and fan-in each raise it (both remove the cheap remedy), and *nothing* may breach it. Mechanical work floors at **tier 0 regardless of blast** — a schema check doesn't improve on a bigger model. Two downgrade paths, both clamped at the floor (proven clade; budget pressure); **when budget can't afford the floor it escalates to a human rather than silently under-provisioning.** **Review independence is two rules:** *agent* independence is absolute (no self-review, any blast radius), *model* independence is required only **above low blast** — shared model = shared blind spots, but at low blast the task is reversible and cheap to re-run, so model reuse keeps bulk review cheap. Tier decisions carry their **input scores** to the ledger (a computed policy must be inspectable). 23 worker tests, **113 repo-wide**; mutation-verified; added `packages/worker/tsconfig.spec.json` (closes the last P0 typecheck loose end). **Dogfooded the full chain on real models:** tier 0 → no LLM · tier 1 → `qwen3.5:2b` · tier 2 → `gemma4:12b` · proven clade **downgrades 2→1** · high-blast irreversible root **upgrades to Claude**.
- **Doneness gate (`getProjectDoneness`) is the authoritative finish line** — 4 pillars: phases, unsatisfied ACs, open defects, architecture drift. As of P4 it still reports: the four **lifecycle phases** (genesis/requirements/architecture/plan) are `pending`, **29 unsatisfied ACs** across 11 requirements (ACs are not auto-accepted when a phase completes — they must be accepted deliberately), **2 open defects**, and **3 missing architecture components** (drift). All must be cleared before "done" is true.
- **P3.5 complete (phase `f9274b20-…`, ADR-0008 amended):** **admission is granted TO A TIER, not in the abstract.** P3 probed every candidate with `Verdict` + `CapabilityManifest` — both *meta-agent* artifacts (Reviewer / Agent Creator, Tier 2–3) — so it refused Tier-1 candidates for failing a job they'd never be given; `qwen3.5:2b` was rejected on exactly that basis. Probes now carry `logicalTier`, selected by `probesForTier()`. **Tier 1 is judged on `EvidenceBundle`** with *instruction-following* checks (deliverable carries the non-empty `answer` the prompt demanded — unreachable by schema validation, since `deliverable` is `Type.Unknown()` by design). **A tier with no probes raises `NoProbesForTierError`, never `admitted:true`** (Tier 0 is no-LLM). Live: **`qwen3.5:2b` passes tier-1 admission 2 of 3 runs**; 4b and 9b pass. 21 router tests, **92 repo-wide**.
- **⚠ OPEN HIGH DEFECT `d678cd8c` — admission verdicts do not replicate.** Each probe runs once against a stochastic model: `qwen3.5:2b` was **admitted** at Tier 2 in P3.5 where the identical P3 probe set **refused** it. So tier resolution would depend on which sample ran on admission day — *and* the single P3 refusal that motivated tier-scoping was itself weak evidence (the argument rests on ADR-0002's tier definitions, not that run). **Never cite one admission run as evidence about a model.** Three candidate fixes on the defect; settle before **P6** staffs agents from the catalog.
- **P3 complete (phase `dfce4456-…`, [ADR-0008](docs/decisions/ADR-0008-admission-gate-semantic-probes.md)):** `ModelRouter` + structured-output admission gate, dispatching via the **Vercel AI SDK** (`ai` 7 · `@ai-sdk/anthropic` · `@ai-sdk/openai-compatible`). **The headline finding: the gate as R3 literally specifies it does not work.** Backends serve structured output by *constrained decoding*, so schema-valid output is near-certain regardless of capability — a shape-only gate **admitted every model, including a 2B one** that returned a `Verdict` with `outcome:"fail"` and `findings:[]`. Fixed with `semanticChecks` per probe (conditional assertions a schema cannot carry: `minItems` can't say "non-empty only when outcome is fail"). After the fix: `qwen3.5:2b` **refused**, `qwen3.5:9b` **refused**, `gemma4:12b` **admitted** — it discriminates. An earlier run had failed the *opposite* way and looked just as real: `createOpenAICompatible` silently drops the json_schema format without `supportsStructuredOutputs:true`, refusing everything for reasons unrelated to the models. **`CatalogResolver` returns `null` for absence and throws only on real failure**, so a DB outage can't masquerade as "fall back to Claude"; the Tier-2→frontier fallback **reports itself** on `ResolvedModel.fallback`. Models moved a generation: **`qwen3.5:{2b,4b,9b}` + `gemma4:{12b,latest}`** (ADR-0002 amended — no decision changed, models were always data). **Enabled the compose GPU block** — on CPU a 9B candidate didn't finish two probes in ten minutes; on GPU (RTX 3090, ~188 tok/s) the three-model run takes ~160s. 13 router tests, **84 repo-wide**, 20 integration; added `packages/model-router/tsconfig.spec.json` (closes a P0 loose end).
- **P2.5 complete (phase `94e553c6-…`, [ADR-0007](docs/decisions/ADR-0007-r12-r13-sequencing-and-contract-surface.md)):** the R12/R13 **contract surface** in `shared-types`, sequenced *before* P3 because `EvidenceBundle` had **zero consumers outside `shared-types`** (verified) — after P5–P8 it would be a breaking refactor across the meta-agents. Two guarantees moved from prose into the schema: **`WorkerContractView`** (`Type.Omit` + `additionalProperties:false`, own `$id`) makes a view still carrying `verificationPlan` *fail validation*, and **`ReflectionRecord`** declares no `gate`/`outcome`/`verdictId` and cannot acquire them — reflection is structurally incapable of being a verdict. **`ActionRecord.viaBrokerGrantId` is non-nullable** (unlike `ConsultedSource`): context can be granted inline, an action never — there is no unbrokered action. Tool grants live on `TaskContract.inputs.toolEntitlements` **only** (not mirrored to `CapabilityManifest` — one authority). `EvidenceBundle.actions` prose → structured (and lost `minItems:1`: a reasoning-only task honestly takes no actions); `reflection` present-and-nullable. **`action` is its own ledger family** (a lookup, not a string scan); reflection stays under `execution`, keeping `verification` exclusively the Reviewer's. Added actor kind `action_broker`. R12's own open question resolved: **no separate reflection budget** — it spends the contract's, cost *attributed* not capped. **66 shared-types tests (was 39), 72 repo-wide, 20 integration**, build+typecheck clean; mutation-verified (neutering `Type.Omit` failed exactly the 4 withholding tests); dogfooded through the built package against real Postgres, appending and replaying a genuine `family='action'` event. **R12/R13 stay `approved`, NOT `satisfied`** — the runtime halves are P8.5/P8.6.
- **Four agentic patterns adopted (2026-07-26, [ADR-0006](docs/decisions/ADR-0006-agentic-patterns.md)):** reflection · tool use · planning · multi-agent collaboration, recorded as **principles in Tasktracker** (they now surface on every `setActiveTask`). The audit found **two already core, two gaps**. *Planning* is core and **exceeded** — Gate A verifies the plan itself before execution. *Multi-agent* is the premise — the Reviewer is a separate meta-agent, and independence is constitutional. *Reflection* is **partial → R12 (`draft`)**: there is no self-critique before submission, so the first correction opportunity is a Gate B rejection, which is expensive twice over (review often runs a tier *above* the worker, then costs an escalation rung). *Tool use* is **absent → R13 (`draft`)**, the largest gap: `entitlements`/`contextEntitlements` grant **context**, the Context Broker is the sole **context** channel, and `EvidenceBundle.actions` is prose — agents can reason and consult but **cannot act**. Planned as an **Action Broker**, a sibling of the Context Broker. Two constraints carry the decision: **self-review is never self-verification** (no verdict, no skipping Gate B, critique against `acceptanceCriteria` and never the withheld `verificationPlan`) and **tool use is brokered, never direct** (an unmediated call is an unlogged side effect → breaks invariant #1; `blastRadius` gains a second job bounding *which tools are reachable*). No code changed; P0–P2 unaffected.
- **P2 complete (phase `12c6ab84-…`, R2 `satisfied`):** new **6th package `@artifex/memory-fabric`** ([ADR-0005](docs/decisions/ADR-0005-memory-fabric-package.md)) — both `api` and `worker` need the data layer, and putting it in `worker` would invert the plane separation. Holds `migrations/` (node-pg-migrate, up **and** down) + repositories. **Audit Ledger:** `seq bigint GENERATED ALWAYS AS IDENTITY`; append-only enforced by DB triggers rejecting UPDATE/DELETE **and** TRUNCATE (row triggers don't see truncate); `AFTER INSERT` fires `pg_notify('artifex_ledger', …)` with a **pointer** (`seq`/`eventId`/`missionId`/`taskId`/`family`/`type`), not the event — NOTIFY caps at 8000 bytes, so consumers read the row by `seq`. `append()` validates against `LedgerEventInputSchema` **before** writing. No `CHECK` on `family` on purpose (the vocabulary lives in the schema; a DDL copy would drift). **Model Catalog:** one row per logical tier, `admitted` required not defaulted (ADR-0002 gate), unmapped tier raises `TierNotInCatalogError` — never a silent default. Added `ModelCatalogEntry`/`Input` to `shared-types`. Integration-only by nature (no unit-test script); root `test:integration` now fans out to workspaces. **17 integration tests** (20 w/ root harness), 45 unit tests repo-wide; verified the append-only tests bite by removing the trigger; dogfooded against the real compose Postgres.
- **P1 complete (phase `b5a1d118-…`, R1 `satisfied`):** `@artifex/shared-types` now holds the five real TypeBox schemas — `TaskContract` (full dossier anatomy), `LedgerEvent` (+`LedgerEventInput`; monotonic `seq` assigned by the ledger; 8 audit families), `EvidenceBundle` (did/consulted-via-broker-grant/assumed), `Verdict` (Gate A/B, criterionId + errorClass + failing step), `CapabilityManifest` (`logicalTier`, non-empty validation harness) — plus shared vocabularies (`common.ts`) and the ajv validation core (`validate`/`assertValid`/`toJsonSchema`, path-specific errors). **ADR-0004 enforced literally:** `toJsonSchema()` returns the schema object itself (referential identity, asserted in test) — zero translation between what we validate and what we hand a model. All objects `additionalProperties: false`; closed vocabularies render as `enum` (better structured-output support than `anyOf`/`const`). Types are `Static<>` only. TDD red→green; **34 shared-types tests** (40 across the repo) + integration 3/3; dogfooded by importing the built package from `packages/worker`. Added `tsconfig.spec.json` so tests/fixtures are typechecked (the build config excludes them, and vitest doesn't typecheck).
- **P0 scaffold (phase `fb7ebb14-…`, complete):** all-TypeScript npm-workspaces monorepo — `@artifex/shared-types` · `@artifex/model-router` · `@artifex/worker` (ESM libs, vitest) · `@artifex/api` (NestJS 11, jest) · `@artifex/dashboard` (**Angular 22**, `@angular/build:unit-test` = vitest+jsdom, no browser). Strict `tsconfig.base.json`, `.nvmrc`=**24.18.0**, fan-out root scripts (`build`/`test`/`typecheck`/`test:integration`). `docker-compose.yml` (Postgres+pgvector pg17 · Redis 7 · Ollama, all healthchecked; pgvector initdb) + `.env.example` (host-port defaults 5433/6379/11434, overridable via gitignored `.env`). Root testcontainers harness (`test/integration/`). CI at `.github/workflows/ci.yml` (install→build→typecheck→unit→integration). DoD verified: full green sweep + `docker compose up --wait` all healthy + harness 3/3. **No business logic; no invariants implemented (none violated).**
- **Toolchain bump (2026-07-24, post-P0):** Node upgraded 24.14 → **24.18.0** (via `winget upgrade OpenJS.NodeJS.LTS`; npm came along 11.9 → **11.16**), which unlocked the Angular **20 → 22** upgrade. Angular 22 dropped Karma/Chrome for the vitest+jsdom unit-test builder — so CI no longer needs a browser. npm 11.16 now **blocks dependency install scripts by default**; all currently-blocked ones (esbuild is fine w/o them; `@parcel/watcher`/`lmdb`/`ssh2`/`cpu-features` are optional native accelerators) have JS fallbacks — full build/test/integration stays green. Approve via `npm approve-scripts` only if native-perf matters later.
- **v0 plan (ADR-0003):** 11 approved requirements (R1–R11) with 26 acceptance criteria; 14 TDD-shaped phases (P0 infra → P1 schemas → P2 ledger → P3 router → P4 constitution/tier → P5–P8 meta-agents → P9 loop → P10 API → P11 learning seam → P12 dashboard → P13 dogfood), each linked to its requirement. Schema encoding = **TypeBox** (ADR-0004). v0 dogfood mission = "structured report from 2–3 sub-questions". Tier-2 = attempt local 32B with Claude fallback via the admission gate.
- **AI Layer in place (2026-07-23):** per-package `CLAUDE.md` hierarchy · SessionStart/Stop hooks (`.claude/settings.json`) · 5 glob-scoped guardrail skills (`.claude/skills/`) · a working ts-morph **codebase-search MCP** (`tools/codebase-search/`, in `.mcp.json`; `find_symbol`/`find_references`/`list_exports`, auto-builds on `npm install`) · read-only `artifex-explorer` subagent. Guardrails now steer the build. **Convention (also in CLAUDE.md + memory): install deps via the package-manager CLI, never hand-edit `package.json`.**
- **Stack decided (ADR-0001):** all-TypeScript. Angular dashboard · NestJS control plane · **separate** TS agent-runtime worker (BullMQ) · PostgreSQL + pgvector as the whole Memory Fabric · Redis/BullMQ job queue · a provider-neutral **Model Router** dispatching to Claude + local OpenAI-compatible models (Ollama/vLLM).
- **Model/inference decided (ADR-0002, from frozen brainstorm `9885e5b0`):** model **tier is a computed policy** (blast radius + fan-in + reversibility + task class + budget + clade score), **4-tier ladder** (Tier-0 no-LLM → Tier-3 frontier); tier-bump = an escalation-ladder rung; budget-vs-blast governed by the **autonomy dial**. Local models declared by logical tier via a versioned **Model Catalog** (Postgres); v0 workhorse **Qwen2.5-family** (replaceable, admission-gated on real schemas). Serving: **Ollama dev → vLLM staging/prod** behind the router. Learning science loop **TS-now** behind a ledger-projection + proposal-emitter seam; Python only "when it hurts". Owner hardware: single **24 GB GPU** (prod fan-out will need cloud/multi-GPU vLLM).
- **Name:** **Artifex** (Latin, "master craftsman/maker") — chosen 2026-07-22, replacing the working title "SWARM". Fully rebranded: repo, Tasktracker project + brainstorms, README, CLAUDE docs, the `solution/` dossier, and the ADRs. (Dated `history/` entries retain "SWARM" as the accurate record of what the project was called at the time.)
- **Functional design** exists as a **solution dossier (v1.1, 22 July 2026)** in `solution/` (7 HTML pages) — deliberately technology-free.
- **Primary artifact:** a 7-page HTML dossier in `solution/` — Overview (`index.html`), Architecture, Mission Lifecycle, Agents, Memory & Learning, Observability, Risks & Safeguards — all mutually linked.
- **Provenance:** outcome of a structured brainstorm (Socratic clarification → research → Six Hats / SCAMPER / premortem), source in `docs/brainstorms/2026-07-22-agent-swarm.md`, by Martin Hylleberg.
- **Design locked this brainstorm:** domain-neutral missions; maximal atomization (atomicity is itself verified); recursive fold-up integration; contract-first definition of done; escalation-ladder failure handling; earned agent permanence; constitutional self-improvement (immutable metric/review/audit/budget core); per-mission autonomy dial; earned knowledge commons; instance-per-mission + shared brain; mediated context broker; two-speed learning; budgeted swarm; surrender as a first-class outcome; audit-ledger-driven observability cockpit.
- **System of record:** Tasktracker project **"Artifex"** (id `faf7e141-4cad-4e53-ab65-e490cba4e5a5`). Holds: two frozen brainstorms (functional `bd543029`, technical/model `9885e5b0`), a **full architecture model (19 components + 31 relationships** — deployment plane, the meta-layer inside the runtime worker, the memory-fabric stores, and the model tier/catalog pieces), and a genesis phase (id `84676257-…`). Mirrored for contributors in [`ARCHITECTURE.md`](ARCHITECTURE.md). No requirements or implementation phases entered yet.
- **Version control:** git `main` → **public** repo https://github.com/mhylle/artifex (Apache-2.0, © 2026 Martin Hylleberg). Open source.

## Next-Phase Options (user's call)

1. **Continue building — `tt-implement-phase` on P3 (Model Router & admission gate)** — provider-neutral dispatch to Claude + OpenAI-compatible local endpoints, resolving logical tier through the Model Catalog (which now exists, with `TierNotInCatalogError` and the `admitted` gate flag already modelled). Against R3.
2. **Review/adjust the plan first** — the remaining P3–P13 phases and 13 requirements are in Tasktracker; reorder, split, or add before continuing.
3. **Decide where R12/R13 sit** (see the open question below) — both are `draft`, so the plan is unchanged until approved.

## Known Loose Ends

- **R3 AC-3 half-verified:** the Tier-2→Claude fallback is proven at the *resolution* level (returns `anthropic/claude-opus-5`, reports the substitution) but **no live Claude dispatch was exercised** — no `ANTHROPIC_API_KEY` in this environment. Cover it when a key is available (P9 at the latest).
- **`memory-fabric` test fixtures still say `qwen2.5:14b` / `qwen2.5:32b-awq`.** Self-consistent and harmless (they test upsert/resolve, not model identity), but stale as data — update when that package is next touched.
- **The compose GPU block is now enabled** (`docker-compose.yml`). Contributors without the NVIDIA Container Toolkit must comment it back out. Check with `docker run --rm --gpus all --entrypoint nvidia-smi ollama/ollama:latest -L` — the `--entrypoint` override matters, or the image reports `unknown command` and looks like a missing toolkit.

- **RESOLVED 2026-07-26** — R13 enters v0 (owner's call); R12 and R13 are `approved` and split across P2.5 (surface, done) / P8.5 (Action Broker) / P8.6 (self-critique). `EvidenceBundle.actions` is no longer provisional — it went structured in P2.5, in one breaking-change window taken while it was free.
- **Tasktracker ordering caveat:** `reorderTask` reported success for P2.5/P8.5/P8.6, but `getRootTasks` still lists them last — the listing does not appear to sort by `orderIndex`. Sequence is carried by the phase titles and an explicit "Sequencing:" line in each phase body. Don't infer run order from the list.
- **P8.5 must not absorb open defect `8a6ee598`.** The ledger `seq`-gap defect becomes *reachable* there (first concurrent appenders); it stays tracked separately and must not be papered over inside the broker.

- The two brainstorms are frozen in Tasktracker but the functional one also lives as markdown (`docs/brainstorms/`); project `brainstormPolicy` is `optional`.
- CI now exists (`.github/workflows/ci.yml`); still no CONTRIBUTING / issue templates — add later.
- **Deferred from P0:** wire the root workspace tsconfig path aliases into `tools/codebase-search` `loadProject()` so `find_references` resolves cross-package alias imports (do once P1 introduces real cross-package imports).
- 3 moderate npm-audit advisories remain in the dev toolchain after the Angular 22 bump — not force-fixed (breaking); revisit later.
- Only `shared-types` and `memory-fabric` typecheck their tests (`tsconfig.spec.json`); `model-router` and `worker` still exclude theirs from `tsc` — add the same spec config when those packages grow real tests (P3 / P9).
- **Open defect (`8a6ee598-…`, medium):** ledger `seq` is assigned before commit, so under *concurrent* writers a live consumer using `readSince(lastSeq)` can skip an event that commits late. Replay-from-zero is unaffected and v0 appends are serial, so it's latent — but it must be handled when P9 fans out or P12 streams live.
- No requirements exist yet; genesis phase `84676257-…` is an unlinked container.
- ADR-0002 parking lot: prod concurrency/hardware sizing, per-tier cost-weight calibration, catalog-A/B harness, Tier-2-on-24GB viability.
