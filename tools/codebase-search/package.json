{
  "name": "@artifex/codebase-search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AST codebase-search MCP server for the Artifex monorepo (symbol / definition / reference search over TypeScript).",
  "bin": {
    "artifex-codebase-search": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "prepare": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ts-morph": "^28.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
