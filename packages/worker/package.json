{
  "name": "@artifex/worker",
  "version": "0.0.0",
  "main": "dist/index.js",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.spec.json",
    "start": "node dist/index.js"
  },
  "license": "Apache-2.0",
  "type": "module",
  "private": true,
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/pg": "^8.20.0"
  },
  "dependencies": {
    "@artifex/memory-fabric": "^0.0.0",
    "@artifex/model-router": "^0.0.0",
    "@artifex/shared-types": "^0.0.0",
    "@sinclair/typebox": "^0.34.52",
    "bullmq": "^5.81.3",
    "pg": "^8.22.0"
  }
}
