/**
 * @artifex/shared-types — the foundation everything depends on.
 *
 * The TypeBox schemas (task/mission contract, ledger event, evidence bundle,
 * verdict, capability manifest) and the ajv validator helpers are introduced in
 * phase P1 (see ADR-0004). This is a scaffold placeholder only — no schemas yet.
 */
export const PACKAGE_NAME = '@artifex/shared-types';
